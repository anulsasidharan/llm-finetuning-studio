# MEMORY.md — LLM Fine-Tuning Studio
# Claude Code reads this at the start of every session to restore context instantly.
# Update this file after every significant session.

## PROJECT IDENTITY
- Name: LLM Fine-Tuning Studio
- Owner: Anu Sasidharan | OrionVexa (orionvexa.ca)
- GitHub: https://github.com/anulsasidharan/llm-finetuning-studio
- Type: Standalone independent app — shares NOTHING with Unified RAG Studio
- Stack: Next.js 14 + FastAPI + PostgreSQL + Redis + MinIO + Celery

## CURRENT PHASE
- Phase: 2 — Training Engine + Core Modules (Weeks 4–8)
- Active Week: 4
- Last completed task: PHASE2-006 — orpo_trainer.py (2026-06-21).
- Next task: PHASE2-007 — training_engine/utils/callbacks.py (MetricsCallback → Redis pub/sub)

## GIT WORKFLOW
- main branch:     production-ready only — never commit directly
- develop branch:  integration branch — never commit directly
- feature branches: cut from develop, named feat/TASK-ID-description
- all PRs target:  develop (never main)
- develop → main:  only at end of each phase milestone
- PR template:     .github/PULL_REQUEST_TEMPLATE.md
- CI runs on:      push to develop + all PRs

## ARCHITECTURE DECISIONS (DO NOT REVISIT)
- Frontend: Next.js 14 App Router only — never Pages Router
- State: TanStack Query (server) + Zustand (client) + React Hook Form + Zod (forms)
- Python package manager: uv — never bare pip
- DB ORM: SQLAlchemy 2 async — never sync sessions
- Storage: MinIO in dev, S3 in production — client in core/storage.py
- Task queue: Celery with Redis broker — 3 queues: training, export, default
- Logging: structlog JSON — never print()
- Config: pydantic-settings Settings class — never os.environ directly
- Auth: JWT (python-jose) + bcrypt (passlib) — self-contained, no external auth
- bcrypt MUST stay pinned to 4.0.1 in apps/backend/requirements.txt — passlib 1.7.4 (unmaintained) is incompatible with bcrypt>=4.1's stricter 72-byte enforcement (raises ValueError during passlib's internal self-test); do not bump bcrypt without also replacing passlib
- tests/conftest.py's `client` TestClient fixture MUST stay session-scoped, not function-scoped — the async SQLAlchemy engine/pool in core/database.py is a process-wide singleton; a function-scoped fixture gives each test its own asyncio event loop, and connections leak across loops, crashing on Windows+asyncpg with "Event loop is closed". One shared loop for the whole pytest session avoids this entirely
- DB-touching backend tests need DATABASE_URL/DATABASE_URL_SYNC overridden to localhost:5433 (not the .env default of docker-internal host "postgres:5432") when run locally outside Docker via `uv run pytest` — exactly mirrors what .github/workflows/ci.yml's backend-test job already does with its own ephemeral postgres service; no code change needed, just env vars at invocation time
- AppException subclasses (ConflictError, UnauthorizedError, etc. from core/exceptions.py) only produce their declared status_code because main.py registers an `@app.exception_handler(AppException)` → JSONResponse handler; without it they'd bubble up as unhandled 500s
- Model catalog + GPU pricing are DB-backed tables (model_catalog, gpu_pricing — added PHASE1-WEEK2-013), NOT static Python config — chosen because GET /models/catalog and GET /gpu/pricing are planned as real queryable routes. Both are global reference tables (no user_id FK). Seeded idempotently via apps/backend/scripts/seed_data.py using `postgresql.insert(...).on_conflict_do_update(...)` upserts
- Standalone scripts under apps/backend/scripts/ that import `core`/`models` need a `sys.path.insert(0, parent)` bootstrap at the top of the file — `uv run python scripts/foo.py` sets sys.path[0] to the script's own directory, not the invocation cwd, so bare `from core...` imports 404 otherwise (mirrors why alembic.ini has `prepend_sys_path = .`). ruff.toml's `"scripts/*"` per-file-ignore includes `E402` to allow the bootstrap to precede other imports
- `.github/workflows/ci.yml`'s `backend-test` job only runs Postgres + Redis as service containers — there is NO MinIO service in CI. Any test exercising a `core/storage.py` code path must mock the storage call (`monkeypatch.setattr(<module>, "upload_file", fake_fn)` etc.) — never hit a real MinIO endpoint from a test, or CI will fail with connection errors. DB-touching tests are fine hitting real Postgres (CI provides it) via the existing `client` fixture / `psycopg2` cleanup pattern — see tests/test_dataset_routes.py
- NEVER call a second ad-hoc `asyncio.run(...)` against the real `core.database.engine`/AsyncSessionLocal from inside a pytest test function — `tests/conftest.py`'s `client` fixture is session-scoped and holds one event loop open for the *entire* test session; any other asyncio.run() that touches the same process-wide engine/pool will eventually crash with cross-loop asyncpg errors (`Event loop is closed`, `attached to a different loop`), even if you consolidate to one asyncio.run() per test or add engine.dispose(). The only safe ways to hit the real DB from a test: (a) go through the `client` fixture (HTTP calls into the app, which already shares its loop), or (b) for non-HTTP scripts, run them as a real `subprocess.run([sys.executable, "scripts/foo.py"], ...)` (a separate process/engine entirely) and verify results with sync `psycopg2` against `settings.DATABASE_URL_SYNC` — see tests/test_seed_data.py
- `training_engine/__init__.py` was deleted (PHASE1-WEEK3-002) — it was empty/unused (nothing imports `training_engine.*` anywhere) and its mere presence broke `uv run pytest tests/` there: pytest's rootdir package-walk treated `training_engine` itself as a subpackage and walked one level too far up to the monorepo root for `sys.path` insertion, so any `from <local_pkg> import ...` inside a test silently resolved against a same-named pip package instead of the local one. Do not recreate `training_engine/__init__.py` unless something actually needs `training_engine` to be an importable package — if that happens, also add explicit `pythonpath = ["."]` under `[tool.pytest.ini_options]` (no pyproject.toml exists in training_engine yet) so test imports keep resolving to the local subpackages first
- LANDMINE (still unresolved, but did NOT block PHASE1-WEEK3-003): `training_engine/datasets/` (the local Alpaca/ShareGPT/ChatML formatter + quality-check package) has the exact same import name as the pip-installed `datasets==2.19.0` (HuggingFace) library already pinned in `training_engine/requirements.txt`. Neither `formatter.py` nor `quality_check.py` needs `import datasets` (both are pure dict/string transforms over already-loaded rows), so the collision hasn't triggered yet. It is still live for the planned `training_engine/datasets/loader.py` (mentioned in CLAUDE.md's directory structure), which will need `import datasets` for `load_dataset()` — that import will resolve to itself (the local package) once `training_engine` is on `sys.path`, not the HF library. Needs a deliberate decision before that task starts: e.g. alias the HF import (`import datasets as hf_datasets` won't help — it's the same name resolution problem), or rename the local package, or use `importlib.import_module` tricks. Flag this explicitly when starting any `training_engine/datasets/loader.py` work
- `training_engine/datasets/quality_check.py`'s `run_quality_check(rows)` uses `langdetect==1.0.9` (new dependency, added PHASE1-WEEK3-003) for per-row language detection — set `DetectorFactory.seed = 0` at import time, since langdetect's detector is otherwise seeded from a non-deterministic RNG and gives different results across runs/tests for the same input. langdetect is also documented to be unreliable on very short strings (a few words); tests/fixtures needing a specific language code should use full sentences, not short phrases
- `training_engine/requirements.txt` had no `pytest` entry despite the root `Makefile` already invoking `pytest tests/` against it — added `pytest==8.2.0` there plus a new `training_engine/requirements-dev.txt` (mirrors `apps/backend`'s split: `requirements.txt` is the full pinned env freeze including dev tools, `requirements-dev.txt` is the curated direct-dependency list for dev-only tools)
- When a FastAPI route needs logic that lives in `training_engine` (a separate standalone process/environment from `apps/backend` per the top-of-file architecture decision), the established pattern (confirmed with the user, PHASE1-WEEK3-004) is to **port** that logic into `apps/backend/services/` as a near-verbatim copy — not install `training_engine` as a backend dependency (blurs the standalone-process boundary, drags `training_engine`'s deps like `langdetect` and its unresolved `datasets`-naming-collision landmine into a new environment) and not dispatch via Celery (real process separation, but async-polling complexity not worth it for fast/synchronous/CPU-only operations like format-detection or quality-checks). `apps/backend/services/dataset_format.py`/`dataset_quality.py` are deliberate duplicates of `training_engine/datasets/formatter.py`/`quality_check.py` — keep both in sync by hand if either evolves. Apply the same port-don't-import pattern to any future backend route needing `training_engine` logic synchronously.
- `apps/frontend` is Next.js 16.2.9 — `apps/frontend/AGENTS.md` (referenced from `apps/frontend/CLAUDE.md` via `@AGENTS.md`) explicitly warns this version has breaking changes from training-data Next.js knowledge and to read `node_modules/next/dist/docs/` before writing code. Confirmed-still-valid as of PHASE1-WEEK3-006: `usePathname`, `<Link href>`, file-based routing, and route groups `(name)` all work as expected; new in this version is a global `LayoutProps<'/route'>`/`PageProps<'/route'>` helper-type pair (no import needed) generated by `next dev`/`next build`/`next typegen`, keyed by literal route strings — not used yet (skipped for the route-group root layout since the literal key isn't worth the typegen coupling there), but worth reaching for once concrete dynamic routes (e.g. `/datasets/[datasetId]`) need typed `params`.
- `apps/frontend/app/globals.css` was missing the full shadcn neutral theme CSS variable set (`--border`, `--input`, `--ring`, `--primary`, `--secondary`, `--muted`, `--accent`, `--destructive`, `--card`, `--popover`, plus the `--radius-sm/md/lg/xl` scale) despite `components.json` declaring `cssVariables: true` — only `--background`/`--foreground` existed. Fixed in PHASE1-WEEK3-006 by adding the standard token set to `:root` + the existing `prefers-color-scheme: dark` media query (kept that media-query-based dark-mode pattern rather than switching to next-themes' `.dark`-class strategy, since no `ThemeProvider` is wired up yet — `next-themes` is an installed-but-unused dependency). Every `ui/*` component (Button, Avatar, DropdownMenu, etc., all scaffolded Week 1) depends on these tokens; if a future task sees unstyled shadcn components, check these blocks in `globals.css` before assuming a new regression.
- `apps/frontend/app/page.tsx` (the default create-next-app scaffold claiming `/`) was deleted in PHASE1-WEEK3-006 — `/` is now owned by `app/(dashboard)/page.tsx` inside the dashboard route group. Do not recreate a bare `app/page.tsx`; Next.js errors on two `page.tsx` files resolving to the same URL.
- The `.claude/hooks/lint.py` PostToolUse hook also runs ESLint `--fix` on `.ts`/`.tsx`/`.js`/`.jsx` files (walking up from the edited file to the nearest `package.json`), not just `ruff` on Python — confirmed in PHASE1-WEEK3-006. The standing "leaked Bash cwd breaks the hook's relative path resolution" gotcha (previously only observed on the backend) applies equally to `apps/frontend` work.
- `apps/frontend/lib/api.ts`'s JWT access token is read from `window.localStorage` under the key `fts_access_token` (via `getAccessToken()`, guarded with `typeof window === "undefined"` for SSR/server-component safety) — chosen in PHASE1-WEEK3-007 as just the read-side hook point for the axios request interceptor, since real token storage doesn't exist until `useAuth` lands in PHASE1-WEEK3-009. `useAuth` MUST write the access token to this exact same `fts_access_token` localStorage key (not a different name/mechanism) or the interceptor silently stops attaching the Authorization header. The 401 response interceptor in the same file is currently a bare pass-through (no redirect/refresh) — also intentionally left for PHASE1-WEEK3-009 to fill in. No per-endpoint typed helpers exist in `lib/api.ts` yet (user chose base-instance-only scope for PHASE1-WEEK3-007) — add them per-feature as each page lands, once PHASE1-WEEK3-008's `types/index.ts` exists to type response bodies against.
- **Frontend shared types (`apps/frontend/types/index.ts`) are generated from the backend's live OpenAPI spec, not hand-written** — user's explicit choice in PHASE1-WEEK3-008 over manual sync. Pipeline: `apps/backend/scripts/export_openapi.py` (`uv run python scripts/export_openapi.py` from `apps/backend`; mirrors `scripts/seed_data.py`'s `sys.path.insert` bootstrap; `app.openapi()` is pure schema generation, no DB/live-server dependency) writes `app.openapi()` to `apps/frontend/openapi/schema.json` → `npm run generate:types` (in `apps/frontend`; wraps `openapi-typescript ./openapi/schema.json -o ./types/api-schema.d.ts --empty-objects-unknown`) produces `types/api-schema.d.ts` → `types/index.ts` re-exports clean aliases (`UserResponse`, `TokenResponse`, `DatasetResponse`, `FineTuneJobResponse`, `FineTuneJobConfigResponse`) plus derived literal-union types (`DatasetFormat`, `FineTuneJobStatus`, `Methodology`) indexed off those response types so they can't drift from the generated schema. The `--empty-objects-unknown` flag on `generate:types` is required — without it, `dict[str, Any]` fields (`quality_report`, `training_config`) generate as the useless `Record<string, never>` instead of `Record<string, unknown>`. Both `openapi/schema.json` and `types/api-schema.d.ts` are committed to git (not gitignored), since CI/build never runs the backend — regenerate both by hand and commit the diff whenever a backend Pydantic response schema changes; this mirrors the repo's existing Alembic-migration pattern of "generate once, commit, regenerate on change."
- `apps/backend/schemas/job.py`'s `methodology`/`status` and `schemas/dataset.py`'s `format` were tightened from plain `str` to `Literal[...]` in PHASE1-WEEK3-008, specifically so the OpenAPI spec emits real enums for the frontend codegen above to turn into literal unions — confirmed with the user first since it's real backend validation behavior, not just a type annotation (Pydantic now 422s on an invalid value at the schema layer itself, before any service-layer check runs). `JobStatus = Literal["pending", "queued", "running", "completed", "failed", "cancelled"]` — note only `"pending"` is ever actually set by code today (`job_service.JOB_STATUS_PENDING`); the other five are the aspirational lifecycle from ARCHITECTURE.md's training flow diagram, now type-safe for when Phase 2's training pipeline starts setting them. `Methodology` matches `job_service.SUPPORTED_METHODOLOGIES` exactly (sft/lora/qlora/dpo/orpo/rlhf); `DatasetFormat` matches `dataset_format.SUPPORTED_FORMATS` + `dataset_service.UNDETECTED_FORMAT` (alpaca/sharegpt/chatml/unknown). Existing tests only assert `status_code == 422` on invalid methodology, not exact error text, so this change didn't break anything — but if a future schema field needs the same treatment, check whether any test asserts the specific `ValidationError` message before assuming it's a safe swap.
- **Refresh token lives in `localStorage` under `fts_refresh_token`** (a second key alongside `fts_access_token`), NOT an httpOnly cookie — user's explicit choice in PHASE1-WEEK3-009, overriding `ARCHITECTURE.md`'s AUTH FLOW diagram. Reason: `POST /api/v1/auth/refresh` (built in PHASE1-WEEK2-012) takes `refresh_token` in the JSON request body, not a cookie — there is no cookie-setting/reading code anywhere in `core/auth.py` or `api/v1/routes/auth.py`. Implementing the diagram's httpOnly-cookie design would require real backend changes (Set-Cookie on login/refresh, reading from cookies instead of the body, CORS `credentials`/CSRF handling) — a scope increase beyond a frontend hook task. If a future task ever migrates to httpOnly cookies, update `ARCHITECTURE.md`'s diagram is already correct; the code is what needs to change, and both `lib/api.ts`'s token helpers and `hooks/useAuth.ts` would need rework together.
- `apps/frontend/lib/api.ts`'s response interceptor does **silent refresh-and-retry on 401** (not a bare redirect-to-login) — user's choice in PHASE1-WEEK3-009. On a 401 from any path other than `/auth/login`, `/auth/register`, or `/auth/refresh` itself (those are exempt — refresh failing there means real auth failure, not expiry), it calls `POST /auth/refresh` via a **plain `axios.post`**, not the `api` instance — using `api` would recurse through this same interceptor. A module-level `refreshPromise` makes concurrent 401s share one in-flight refresh call (single-flight) rather than firing N parallel refreshes. The retried request is marked with a local `_retry` flag so it can't loop forever if the retry itself 401s again. If the refresh call fails (refresh token itself expired/invalid), `clearTokens()` then hard-redirects via `window.location.href = "/login"` — deliberately not a Next.js router push, since this module runs outside the React tree and has no access to `next/navigation`'s router.
- `apps/frontend/app/providers.tsx` (`QueryClientProvider`, wired into `app/layout.tsx`) was added in PHASE1-WEEK3-009 — `@tanstack/react-query` had been an installed dependency since Week 1 but nothing had wired up a provider until `hooks/useAuth.ts` became the first real consumer. Any future hook using `useQuery`/`useMutation` can now rely on this provider already being present at the root.
- `apps/frontend/components/layout/Header.tsx`'s account dropdown was extracted into `components/layout/AccountMenu.tsx` (a `"use client"` component calling `useAuth()`) in PHASE1-WEEK3-009 — `Header` itself stays a server component, continuing the PHASE1-WEEK3-006 pattern of isolating interactivity/client-only hooks into a small leaf component rather than converting the whole header to `"use client"`.
- No browser-automation tool (Playwright, etc.) is available in this environment as of PHASE1-WEEK3-009 — confirmed via `ToolSearch`. For frontend tasks where the meaningful behavior is client-side-interactive only (not visible in raw server-rendered HTML, e.g. `useAuth`'s login→401→refresh→retry flow), verification falls back to: (a) `npm run lint` / `npm run build` for compile-time correctness, (b) direct `curl` calls against a locally-run backend to confirm the live API contract matches what the frontend code assumes, and (c) where it matters, a **temporary** Node script (deleted before committing) that imports the real frontend module directly via `node --experimental-strip-types` with a minimal `window`/`localStorage` shim, to exercise the actual shipped code end-to-end against the live backend rather than a reimplementation of its logic. This is the established fallback going forward whenever a UI task's critical path can't be observed in static HTML and no browser tool is available — say so explicitly rather than claiming full browser verification.
- **This codebase's shadcn/ui setup is built on base-ui (`@base-ui/react`), which uses a `render` prop, NOT the conventional shadcn `asChild` prop** — confirmed PHASE1-WEEK3-011 via `components/ui/dialog.tsx`'s existing `<DialogPrimitive.Close render={<Button variant="outline" />}>Cancel</DialogPrimitive.Close>` usage. To make a styled `Button`/`Badge`/etc. render as a different element (e.g. a Next.js `<Link>`), write `<Button render={<Link href="..." />}>label</Button>` — the wrapping component's own children become the rendered children, and its className/props merge onto the passed element via `useRender`/`mergeProps` (see `components/ui/badge.tsx`, `components/ui/button.tsx`). Writing `<Button asChild><Link>...</Link></Button>` (the shadcn-on-Radix convention) compiles but is the wrong API for this codebase — `asChild` isn't a real prop here, and nesting a real `<Link>` inside `Button`'s real `<button>` would also be invalid HTML once it did type-check correctly elsewhere.
- `apps/backend/services/dataset_service.py` already had a `get_dataset(dataset_id, user, db)` function (added PHASE1-WEEK3-004 for internal use by `format_dataset`/`quality_check_dataset`) before any route exposed it externally — PHASE1-WEEK3-011 added the thin `GET /datasets/{dataset_id}` route wrapping it. If a future task needs a service function exposed as a route, check whether the service layer already has it before writing new logic.
- **`GET /datasets/{id}/preview` does NOT exist** despite being listed in CLAUDE.md section 5's route table — confirmed not built as of PHASE1-WEEK3-011 (user explicitly chose to skip it and `DatasetPreview.tsx` rather than guess at scope). Flagged as a backend follow-up, not yet scheduled in BACKLOG.md. Don't assume CLAUDE.md's route table is 1:1 with what's actually implemented — `apps/backend/api/v1/routes/datasets.py` is the source of truth.
- Backend env vars needed to run `uv run uvicorn` locally (outside Docker) point at Docker-internal hostnames by default and need overriding — confirmed the exact var names PHASE1-WEEK3-011: `DATABASE_URL`/`DATABASE_URL_SYNC` (already documented), plus newly confirmed `REDIS_URL="redis://:fts_redis_dev_2024@localhost:6380/0"` (note the password — `.env`'s default has no auth issue locally because it targets the in-Docker hostname; the host-mapped port still requires the password) and, for MinIO, **`MINIO_HOST=localhost` + `MINIO_PORT=9000`** — NOT `MINIO_ENDPOINT` (that var is read by `core/config.py` but never actually consumed by `core/storage.py`'s `Minio(...)` client construction, which builds its own `f"{settings.MINIO_HOST}:{settings.MINIO_PORT}"` host string instead). Setting only `MINIO_ENDPOINT` for local runs silently does nothing and `/health` keeps reporting `storage: "down"`.
- **Any FastAPI path param matching a real `model_catalog.model_id`-style value (e.g. `meta-llama/Meta-Llama-3-8B`) MUST use the `{model_id:path}` converter, not a plain `{model_id}`** — confirmed PHASE1-WEEK3-012 building `GET /api/v1/models/catalog/{model_id:path}`. A plain `{model_id}` only matches a single path segment (no `/`), so a real HuggingFace-style model ID would either 404 or get split across extra path segments. Verified live against the real seeded value, not just a slash-free test fixture. Apply the same `:path` converter to any future route taking a model_id (or similarly slash-containing identifier) as a path param.
- **`.github/workflows/ci.yml`'s `backend-test` job never runs `scripts/seed_data.py`** — confirmed PHASE1-WEEK3-012. Any test exercising `model_catalog`/`gpu_pricing` rows (reference tables seeded only by that script) must insert and clean up its own rows directly via `psycopg2` rather than assuming seeded data is present — see `tests/test_model_catalog_routes.py`'s `seeded_models` fixture. This mirrors the standing "no MinIO in CI" rule for the same reason: CI's Postgres service starts empty beyond what Alembic migrations create.
- **`BaseTrainer` is the training_engine foundation** — confirmed PHASE2-001: abstract class in `training_engine/trainers/base_trainer.py` accepts the same flat `training_config` dict the backend/`ConfigBuilder` produce, normalized internally via frozen `TrainerConfig.from_dict()` (required keys: `learning_rate`, `num_epochs`, `batch_size`; optional extras + method-specific keys with defaults). Constructor takes pre-loaded `dataset_rows: list[dict]` (Celery caller loads from MinIO — no `datasets/loader.py` yet, deliberately avoids the HF `datasets` pip-package naming collision). Shared helpers: `resolve_device()` (cuda/mps/cpu), `load_tokenizer()`/`load_model()` (default full-precision causal LM — subclasses override for QLoRA/PEFT), `build_training_arguments()` (maps flat config → HF `TrainingArguments`), `prepare_dataset_rows()` (ChatML via local `datasets.formatter.to_chatml`; pass `dataset_format="unknown"` to auto-detect), `get_callbacks()` (MetricsCallback lands PHASE2-007). Abstract `train() -> dict`. Raises `TrainerError` on bad inputs. Tests mock `transformers.AutoTokenizer`/`AutoModelForCausalLM` — never hit real GPU/network in unit tests.
- **`utils/hf_datasets.py` resolves the HF `datasets` naming collision for TRL imports** — confirmed PHASE2-002: the local `training_engine/datasets/` package shadows the pip `datasets==2.19.0` library on `sys.path`, which breaks `from trl import SFTTrainer` (TRL itself does `from datasets import Dataset`). `ensure_hf_datasets_loaded()` temporarily removes the local `datasets` entry from `sys.path`/`sys.modules`, imports the pip package, re-registers it as `sys.modules["datasets"]`, and preserves already-loaded local submodules (`datasets.formatter`, `datasets.quality_check`) so `BaseTrainer.prepare_dataset_rows()` keeps working. Any trainer that imports TRL (SFT, LoRA, DPO, ORPO) must call this helper before importing TRL. Unit tests mock TRL/HF Dataset entirely — never exercise the real swap in pytest.
- **`SFTTrainer` (training_engine) wraps `trl.SFTTrainer` for full-weight SFT** — confirmed PHASE2-002: `training_engine/trainers/sft_trainer.py` subclass of `BaseTrainer`, requires `methodology="sft"`. `train()` loads tokenizer/model via base helpers, normalizes rows to ChatML, builds an in-memory HF `Dataset.from_list()`, applies `tokenizer.apply_chat_template` via TRL's `formatting_func`, runs TRL SFTTrainer with `packing=False` and `max_seq_length` from `TrainerConfig`, saves to `{output_dir}/final`, returns `{status, methodology, job_id, output_dir, model_dir, num_rows, metrics}`. 3 new unit tests mock `_get_trl_sft_trainer`/`get_hf_dataset_class`/`AutoTokenizer`/`AutoModelForCausalLM` — full suite 41/41 passing.
- **`LoRATrainer` (training_engine) wraps PEFT LoRA + `trl.SFTTrainer`** — confirmed PHASE2-003: `training_engine/trainers/lora_trainer.py` subclass of `BaseTrainer`, requires `methodology="lora"` and validates `lora_r`/`lora_alpha` at init (mirrors `job_service.py`'s required keys). `train()` loads base model via `BaseTrainer.load_model()`, applies `peft.LoraConfig` + `get_peft_model` with default LLaMA-style `target_modules` and `lora_dropout=0.05`, then reuses the SFT ChatML pipeline (`prepare_dataset_rows` → HF `Dataset.from_list` → `formatting_func` via `apply_chat_template`) with TRL SFTTrainer on the PEFT-wrapped model. Saves adapter artifacts to `{output_dir}/final`, returns the same status/metrics dict shape as `SFTTrainer`. Lazy-imports PEFT via `_get_peft_modules()` so unit tests can mock without loading real PEFT. 4 new unit tests mock `_get_peft_modules`/`_get_trl_sft_trainer`/`get_hf_dataset_class`/`AutoTokenizer`/`AutoModelForCausalLM` — full suite 45/45 passing.
- **`QLoRATrainer` (training_engine) wraps 4-bit NF4 BitsAndBytes + PEFT LoRA + `trl.SFTTrainer`** — confirmed PHASE2-004: `training_engine/trainers/qlora_trainer.py` subclass of `LoRATrainer`, requires `methodology="qlora"` (calls `BaseTrainer.__init__` directly to bypass `LoRATrainer`'s lora-only check), validates `lora_r`/`lora_alpha` at init. Overrides `load_model()` to apply `transformers.BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4", bnb_4bit_compute_dtype=torch.float16, bnb_4bit_use_double_quant=True)` before the inherited LoRA `train()` pipeline (`_apply_lora` → ChatML SFT). Lazy-imports BnB config via `_get_bitsandbytes_config_cls()` for testability. 4 new unit tests mock `_get_bitsandbytes_config_cls`/`_get_peft_modules`/`_get_trl_sft_trainer`/`get_hf_dataset_class`/`AutoTokenizer`/`AutoModelForCausalLM` — full suite 49/49 passing.
- **`DPOTrainer` (training_engine) wraps `trl.DPOTrainer` for preference alignment — subclasses `BaseTrainer` directly, NOT `LoRATrainer`** — confirmed PHASE2-005: `apps/backend/services/job_service.py`'s `METHOD_SPECIFIC_REQUIRED_KEYS["dpo"]` is just `{"beta"}` (no `lora_r`/`lora_alpha`), so `training_engine/trainers/dpo_trainer.py` requires `methodology="dpo"` and validates `self.config.beta is not None` at init, no PEFT involvement at all — matches CLAUDE.md section 7's "2× LoRA VRAM" note, which comes from loading a second full frozen reference-model copy, not from adapters. `train()` calls `self.load_model()` **twice** (policy `model` + frozen `ref_model`), both passed into `trl.DPOTrainer(model=, ref_model=, beta=self.config.beta, max_length=self.config.max_seq_length, max_prompt_length=self.config.max_seq_length // 2, ...)`. **DPO/ORPO dataset rows are preference pairs `{"prompt", "chosen", "rejected"}`, not ChatML conversations** — `BaseTrainer.prepare_dataset_rows()`/`datasets.formatter.to_chatml()` only handle alpaca/sharegpt/chatml single-conversation shapes and do not apply to pairs. Added a parallel `prepare_preference_rows()` + `REQUIRED_PAIR_KEYS = ("prompt", "chosen", "rejected")` directly in `dpo_trainer.py` that validates raw rows have all three keys (raises `TrainerError` listing the missing ones) without going through the formatter at all. `trl==0.8.6` is pinned in `training_engine/requirements.txt`; trl itself is NOT installed in this dev environment (confirmed via `python -c "import trl"` → `ModuleNotFoundError`), so the `DPOTrainer(model, ref_model, beta, args, train_dataset, tokenizer, max_length, max_prompt_length, callbacks)` constructor signature was confirmed from trl 0.8.x's known API surface (pre-`DPOConfig` era), not a live signature inspection — tests mock TRL entirely so this doesn't block correctness today, but **flag this explicitly if ORPO (PHASE2-006) or any future TRL-version-sensitive trainer needs the same treatment** — verify against real `trl` source/docs before assuming an API shape, since trl's API changed significantly between 0.8.x and later 0.9+/DPOConfig versions. 5 new unit tests in `test_dpo_trainer.py` (rejects non-dpo methodology, rejects missing beta, rejects pair row missing required keys via `prepare_preference_rows()`, `train()` asserts `model`/`ref_model` are two distinct loaded instances + correct `beta`/`max_length`/`max_prompt_length` kwargs, callbacks passed through) — full suite 54/54 passing.
- **`ORPOTrainer` (training_engine) wraps `trl.ORPOTrainer` for single-pass SFT + odds-ratio preference alignment — subclasses `BaseTrainer` directly, no reference model unlike DPO** — confirmed PHASE2-006: `apps/backend/services/job_service.py`'s `METHOD_SPECIFIC_REQUIRED_KEYS["orpo"]` is `{"beta"}` (same single key as DPO), so `training_engine/trainers/orpo_trainer.py` requires `methodology="orpo"` and validates `self.config.beta is not None` at init — no PEFT/LoRA involvement. Reused DPO's preference-pair validation pattern verbatim: `prepare_preference_rows()` + local `REQUIRED_PAIR_KEYS = ("prompt", "chosen", "rejected")`, duplicated in `orpo_trainer.py` rather than imported from `dpo_trainer.py` (siblings, not inheritance — matches the existing duplication style elsewhere in the repo, e.g. `dataset_format.py`/`dataset_quality.py`). **Critical API difference from DPO, confirmed by fetching the real `trl` v0.8.6 source from GitHub (trl is NOT installed in this dev env) since the PHASE2-005 memory flagged this as unverified:** `trl.ORPOTrainer.__init__` does NOT take `beta`/`max_length`/`max_prompt_length`/`ref_model` as direct constructor kwargs at all — only `model=, args=, train_dataset=, tokenizer=, callbacks=, peft_config=, ...`. Those four values live on an `ORPOConfig` (subclass of `TrainingArguments`, default `beta=0.1`) passed as `args=`. `orpo_trainer.py` only calls `self.load_model()` **once** (no `ref_model`) and builds args via a new `_build_orpo_args()` method (mirrors `BaseTrainer.build_training_arguments()`'s fields plus `beta`/`max_length=self.config.max_seq_length`/`max_prompt_length=self.config.max_seq_length // 2`), using lazy-import helpers `_get_trl_orpo_trainer()` and `_get_trl_orpo_config()` (both call `ensure_hf_datasets_loaded()` first, same as DPO). 5 new unit tests in `test_orpo_trainer.py` (rejects non-orpo methodology, rejects missing beta, rejects pair row missing required keys, `train()` asserts exactly one `from_pretrained` call + `ORPOConfig` kwargs correct + no `ref_model`/`beta` passed directly to `ORPOTrainer` + callbacks passed through) — full suite 59/59 passing. **If a future trainer needs a real `trl` API shape and `trl` isn't installed locally, fetch the actual pinned-version source from GitHub first (`raw.githubusercontent.com/huggingface/trl/v<pinned-version>/...`) rather than relying on trained-in knowledge of trl's API — it changed significantly across 0.8.x/0.9+/DPOConfig-era versions, and this session's WebFetch catch (ORPO's args-object pattern, no ref_model) would have been a real bug if assumed from memory alone.**
- **`ParameterTooltip` uses Popover, not Tooltip** — confirmed PHASE1-WEEK3-013: despite the CLAUDE.md component name, the user chose click-to-open `components/ui/popover.tsx` over hover `tooltip.tsx` for richer multi-sentence education copy. `ParameterInfo` carries `{label, description, typicalDefault, whyItMatters}`; all entries live in a central `PARAMETER_INFO: Record<string, ParameterInfo>` map in `components/training/ParameterTooltip.tsx` (same data-export pattern as `METHODOLOGY_INFO`). `ParameterField.tsx` takes an optional `info?: ParameterInfo` prop rendered next to the label; `ConfigBuilder.tsx` passes `info={PARAMETER_INFO.<field>}` for all 11 training-config fields. Trigger is a ghost `Button size="icon-xs"` with `PopoverTrigger render={<Button ... />}` (base-ui `render` prop, not `asChild`) + lucide `Info` icon + `aria-label="What is <label>?"`. Popover click interaction can't be verified in this environment (no browser tool) — SSR HTML check for those aria-labels on `/config` is the standing fallback. — confirmed PHASE1-WEEK3-013: despite the CLAUDE.md component name, the user chose click-to-open `components/ui/popover.tsx` over hover `tooltip.tsx` for richer multi-sentence education copy. `ParameterInfo` carries `{label, description, typicalDefault, whyItMatters}`; all entries live in a central `PARAMETER_INFO: Record<string, ParameterInfo>` map in `components/training/ParameterTooltip.tsx` (same data-export pattern as `METHODOLOGY_INFO`). `ParameterField.tsx` takes an optional `info?: ParameterInfo` prop rendered next to the label; `ConfigBuilder.tsx` passes `info={PARAMETER_INFO.<field>}` for all 11 training-config fields. Trigger is a ghost `Button size="icon-xs"` with `PopoverTrigger render={<Button ... />}` (base-ui `render` prop, not `asChild`) + lucide `Info` icon + `aria-label="What is <label>?"`. Popover click interaction can't be verified in this environment (no browser tool) — SSR HTML check for those aria-labels on `/config` is the standing fallback.
- **`tests/test_auth.py::test_decode_token_raises_on_tampered_token`'s old flakiness (noted across PHASE1-WEEK2-011/PHASE1-WEEK2-012 as "confirmed flaky, passed on rerun") had a real, deterministic root cause — not test-isolation/event-loop flakiness like the other flaky-test patterns in this file.** It tampered the *last* character of the JWT via `token[:-1] + ("a" if ... else "b")`. For HS256, the 32-byte signature's base64url encoding ends in a 3-character group representing only 16 real bits (2 bytes); the final character's bottom 2 bits are unused padding, and `python-jose`'s decoder is permissive about their value — so roughly **~7% of the time** (measured empirically: 141/2000 trials), flipping only the last character produces a *different string that decodes to the byte-for-byte identical signature*, meaning the token genuinely still verifies and `decode_token` correctly does NOT raise. This is not a `core/auth.py` bug — `decode_token` already does the textbook `except JWTError: raise HTTPException(401, ...)` pattern. **Fixed for real in this session by changing the test** to tamper a non-boundary character (`token[-5]`) instead of the last one — middle base64url characters always encode a full 6 bits with no padding ambiguity, so this is deterministic (0/2000 failures in the same stress test, confirmed via 5 manual reruns too). If a similar "JWT tamper test flakes occasionally" report ever resurfaces elsewhere, check whether it's tampering the very last character of a base64url segment before assuming the production code is at fault.

## PORT MAP (memorize — never change)
- Frontend:      localhost:3000  (container: fts_frontend)
- Backend API:   localhost:8000  (container: fts_backend)
- PostgreSQL:    localhost:5433  (container: fts_postgres)   ← 5433 not 5432
- Redis:         localhost:6380  (container: fts_redis)      ← 6380 not 6379
- MinIO API:     localhost:9000  (container: fts_minio)
- MinIO Console: localhost:9001  (container: fts_minio)
- Flower:        localhost:5555  (container: fts_flower)

## DOCKER FACTS
- Compose project name: llm-finetuning-studio
- Network: fts_network (bridge, isolated)
- All volumes prefixed: fts_postgres_data, fts_redis_data, fts_minio_data, fts_hf_cache
- All containers prefixed: fts_*
- GPU training: docker compose -f docker-compose.yml -f docker-compose.gpu.yml up

## DIRECTORY SHORTCUTS
- Frontend root:    apps/frontend/
- Backend root:     apps/backend/
- ML engine root:   training_engine/
- API routes:       apps/backend/api/v1/routes/
- DB models:        apps/backend/models/
- Pydantic schemas: apps/backend/schemas/
- Business logic:   apps/backend/services/
- Celery tasks:     apps/backend/tasks/
- DB migrations:    apps/backend/migrations/versions/
- React components: apps/frontend/components/
- Next.js pages:    apps/frontend/app/(dashboard)/
- Custom hooks:     apps/frontend/hooks/
- Shared types:     apps/frontend/types/index.ts

## ENVIRONMENT
- .env is at repo root — never commit it
- DATABASE_URL uses host alias "postgres" inside Docker, "localhost:5433" outside
- REDIS_URL uses host alias "redis" inside Docker, "localhost:6380" outside
- HF_CACHE_DIR=/app/.cache/huggingface (shared volume fts_hf_cache)

## FINE-TUNING METHODS SUPPORTED
SFT → trl.SFTTrainer
LoRA → peft.LoraConfig + SFTTrainer
QLoRA → LoRA + BitsAndBytesConfig(load_in_4bit=True, bnb_4bit_quant_type="nf4")
DPO → trl.DPOTrainer (needs chosen/rejected pairs, doubles VRAM for reference model)
ORPO → trl.ORPOTrainer (single-pass, no reference model, lower VRAM than DPO)
RLHF → trl.PPOTrainer + RewardTrainer (Phase 4 stretch goal)

## BASE MODELS IN CATALOG
meta-llama/Meta-Llama-3-8B, meta-llama/Meta-Llama-3-8B-Instruct
meta-llama/Meta-Llama-3-70B
mistralai/Mistral-7B-v0.3, mistralai/Mistral-7B-Instruct-v0.3
microsoft/Phi-3-mini-4k-instruct, microsoft/Phi-3-medium-4k-instruct
Qwen/Qwen2-7B, Qwen/Qwen2-72B
google/gemma-2-9b, google/gemma-2-27b
codellama/CodeLlama-7b-hf, codellama/CodeLlama-34b-hf

## DATASET FORMATS
alpaca:   {"instruction": "...", "input": "...", "output": "..."}
sharegpt: {"conversations": [{"from": "human", "value": "..."}, {"from": "gpt", "value": "..."}]}
chatml:   {"messages": [{"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]}

## WEBSOCKET PATTERN
Channel: /ws/training/{job_id}
Redis pub/sub channel: training_metrics:{job_id}
Callback: training_engine/utils/callbacks.py → MetricsCallback extends TrainerCallback
Payload type fields: metrics_update | status_change

## SESSION LOG (append after each session)
| Date | What was done | Files changed | Next task |
|------|--------------|---------------|-----------|
| 2026-06-15 | PHASE1-WEEK1-001: Full project infrastructure setup — 62 dirs, all Docker services, Next.js 14 + shadcn, FastAPI + uv, training_engine bootstrap, health endpoint confirmed | docker-compose.yml, all Dockerfiles, .env.example, .gitignore, Makefile, apps/backend/main.py, core/config.py, alembic.ini, start.sh, 79 files committed | PHASE1-WEEK1-002: Makefile verification + README.md |
| 2026-06-17 | PHASE1-WEEK1-002: Verified Makefile targets (help/ps/logs/lint) by running underlying commands directly — `make` binary not installed in this Windows/Git Bash env, so verification was done via `docker compose ps/logs` and `uv run ruff check` on backend + training_engine (both clean); no Makefile fixes needed. Wrote full README.md (badges, features, tech stack, quickstart, service URLs, fine-tuning methods, project structure, dev commands, contributing, license placeholder) | README.md | PHASE1-WEEK2-001: core/config.py full implementation |
| 2026-06-17 | PHASE1-WEEK2-001: Extended `Settings` to cover every var in `.env.example` (App/Postgres/Redis/MinIO/AWS S3/HuggingFace/Cloud GPU Vendors/GPU Pricing/Notifications/Training Engine), added `get_settings()` w/ `lru_cache`. Found+fixed a latent bug: `env_file=".env"` was cwd-relative and broke when running `uv run` from `apps/backend` (no local `.env` there); switched to an absolute path resolved from `config.py`'s own location (`Path(__file__).resolve().parents[3] / ".env"`). Verified settings import cleanly, `ruff check` clean, confirmed via `git stash` that remaining `ruff format` mismatches (13 files) pre-exist on develop and are unrelated. No `os.environ` usage anywhere in apps/backend | apps/backend/core/config.py | PHASE1-WEEK2-002: core/database.py async SQLAlchemy engine + session factory |
| 2026-06-17 | PHASE1-WEEK2-002: Created `core/database.py` — async engine via `create_async_engine(settings.DATABASE_URL, echo=settings.ENVIRONMENT == "development", pool_pre_ping=True)`, `AsyncSessionLocal` (`async_sessionmaker`, `expire_on_commit=False`), declarative `Base(DeclarativeBase)`, and `get_db()` FastAPI dependency yielding an `AsyncSession` and closing it in `finally`. Verified import, `ruff check` and `ruff format --check` both clean. Pre-commit hooks (ruff lint/format on backend) passed automatically. Pushed branch; PR not opened (left for manual creation per established workflow) | apps/backend/core/database.py | PHASE1-WEEK2-003: all ORM models (user, fine_tune_job, dataset, experiment, model_registry) |
| 2026-06-18 | PHASE1-WEEK2-003: Wrote SQLAlchemy 2.0 typed declarative ORM models for all six tables — `User`, `Dataset`, `FineTuneJob`, `Experiment`, `ExperimentRun`, `ModelRegistry` — each in its own file under `apps/backend/models/`, all inheriting `Base` from `core.database`. UUID PKs via `postgresql.UUID(as_uuid=True)` with `server_default=func.gen_random_uuid()` (built into PG13+, no pgcrypto extension needed). JSONB for `training_config`/`quality_report`/`metrics`. All FKs to `users.id` use `ondelete="CASCADE"`; `dataset_id`/`fine_tune_job_id` optional FKs (on `FineTuneJob`/`ModelRegistry`) are plain nullable FKs without cascade per spec. Added the four named indexes (`idx_datasets_user`, `idx_jobs_user_id`, `idx_jobs_status`, `idx_registry_user`) via `index=True` on the respective columns. Wired bidirectional `relationship()`s using `TYPE_CHECKING`-only imports to avoid circular imports. `models/__init__.py` re-exports every class. Verified the exact import line from acceptance criteria succeeds, `ruff check .` and `ruff format --check models/` both clean (pre-commit hook also passed automatically on commit). Pushed branch; PR not opened (left for manual creation per established workflow) | apps/backend/models/{user,dataset,fine_tune_job,experiment,model_registry,__init__}.py | PHASE1-WEEK2-004: Alembic config + initial migration |
| 2026-06-18 | PHASE1-WEEK2-004: Wired `migrations/env.py` to ORM metadata — `from core.database import Base` + explicit import of all six model classes from `models` (so `Base.metadata` is fully populated), `target_metadata = Base.metadata` replacing `None`. Found PHASE1-WEEK2-003's claim that `index=True` produced the four spec-named indexes was wrong — plain `index=True` autogenerates SQLAlchemy default names (`ix_<table>_<column>`), not `idx_datasets_user`/`idx_jobs_user_id`/`idx_jobs_status`/`idx_registry_user`. Fixed by replacing `index=True` with explicit `Index("idx_...", "column")` entries in `__table_args__` on `Dataset`, `FineTuneJob` (two indexes: user_id + status), and `ModelRegistry`. Ran `uv run alembic revision --autogenerate -m "initial schema"` from `apps/backend` with `DATABASE_URL_SYNC=postgresql://fts_user:fts_dev_password_2024@localhost:5433/fts_db` (note: `.env`'s `DATABASE_URL_SYNC` uses Docker-internal host `postgres:5432`, not reachable from the host — must override when running alembic outside Docker) → generated `migrations/versions/c631a2aed670_initial_schema.py` with all 6 `create_table`s, all 4 named indexes, and correct `ondelete="CASCADE"` FKs. Reviewed by hand — no unwanted drops/renames (first migration). Applied `alembic upgrade head`, confirmed via `docker exec fts_postgres psql -c "\dt"` and `"\di"` that all 7 relations (6 tables + alembic_version) and all 4 named indexes exist. Verified `downgrade base` → `upgrade head` round-trip succeeds cleanly. `ruff check .` and `ruff format --check migrations/` both clean after auto-fixing import sort + trailing whitespace in the generated file (these rules are NOT in the `migrations/*` per-file-ignore list — only `E402`/`F401` are ignored there). Backend test suite (2 tests) still passes. Pushed branch; PR not opened (left for manual creation per established workflow). **Gotcha discovered:** the Bash tool's cwd persists across calls within a session — using `cd apps/backend && cmd` directly (not in a subshell) leaks the cwd change forward and breaks the `.claude/hooks/lint.py` PostToolUse hook (it resolves paths relative to cwd). Always wrap `cd`-then-run sequences in a subshell `(cd dir && cmd)` to keep the outer shell at repo root | apps/backend/migrations/env.py, apps/backend/migrations/versions/c631a2aed670_initial_schema.py, apps/backend/models/{dataset,fine_tune_job,model_registry}.py | PHASE1-WEEK2-005: core/security.py bcrypt password hashing |
| 2026-06-18 | PHASE1-WEEK2-005: Created `core/security.py` — `pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")`, `hash_password(password: str) -> str`, `verify_password(plain_password: str, hashed_password: str) -> bool`, exactly per spec. Hit a real bug while running the new tests (not flaky): passlib 1.7.4 (unmaintained since 2020) is incompatible with bcrypt>=4.1 — passlib's internal backend self-test (`detect_wrap_bug`) hashes a 72+ byte secret on first use, and bcrypt 4.1+ raises `ValueError: password cannot be longer than 72 bytes` instead of bcrypt's old silent-truncate behavior. `requirements.txt` had `bcrypt==5.0.0` pinned, tripping this on every call. Fixed by pinning `bcrypt==4.0.1` (last version confirmed compatible with passlib's self-test) and reinstalling via `uv pip install -r requirements.txt`. Added 3 unit tests in `tests/test_security.py` (hash differs from input, verify succeeds on correct password, verify fails on wrong password) — full suite now 5/5 passing. `ruff check .` clean; `ruff format --check core/ tests/` flagged 5 pre-existing unformatted files unrelated to this task (`core/__init__.py`, `core/config.py`, `core/database.py`, `tests/conftest.py`, `tests/test_health.py` — consistent with the 13-file pre-existing mismatch noted in PHASE1-WEEK2-001); the 2 new files are clean. Pre-commit hook passed automatically. Pushed branch; PR not opened (manual creation per established workflow). **Gotcha repeated:** a bare `cd apps/backend && cmd` (no subshell parens) leaked cwd forward again and broke the next Edit's lint hook — had to manually `cd` back to repo root. Reinforces: always use `(cd dir && cmd)` | apps/backend/core/security.py, apps/backend/tests/test_security.py, apps/backend/requirements.txt (bcrypt 5.0.0 → 4.0.1) | PHASE1-WEEK2-006: core/auth.py JWT create/verify + get_current_user dependency |
| 2026-06-18 | PHASE1-WEEK2-006: Created `core/auth.py` — `oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login")`, `create_access_token`/`create_refresh_token` via `jose.jwt.encode` against `settings.SECRET_KEY`/`settings.ALGORITHM` (expiry from `settings.ACCESS_TOKEN_EXPIRE_MINUTES`/`settings.REFRESH_TOKEN_EXPIRE_DAYS`), `decode_token` via `jose.jwt.decode` catching `JWTError` and re-raising a shared `CREDENTIALS_EXCEPTION = HTTPException(401)` (no `core/exceptions.py` yet — deferred to PHASE1-WEEK2-009), and `get_current_user` (`Depends(oauth2_scheme)` + `Depends(get_db)`) resolving the `sub` claim to a UUID and loading the `User` via `db.scalar(select(User).where(...))`, raising 401 on any failure. Added 5 unit tests in `tests/test_auth.py` (access + refresh round-trip, expired token raises 401, tampered token raises 401, wrong-signature token raises 401) — full suite now 10/10 passing. `ruff check .` clean; `ruff format --check core/ tests/` flagged 7 pre-existing unformatted files unrelated to this task (`core/__init__.py`, `core/config.py`, `core/database.py`, `core/security.py`, `tests/conftest.py`, `tests/test_health.py`, `tests/test_security.py` — grew from the 5 noted in PHASE1-WEEK2-005, but confirmed via `git status`/`git diff` that none were touched this session, so the drift is pre-existing on develop); the 2 new files are clean. Pre-commit hook passed automatically. Pushed branch; PR not opened (manual creation per established workflow). **New gotcha:** the PostToolUse lint/format hook runs after *every* Edit/Write call individually — adding an import in one `Edit` and its only usage in a later, separate `Edit` let the hook's ruff auto-fix strip the import as "unused" in between; had to re-add it. Prefer making the import + its first usage in the same tool call, or verify the file afterward | apps/backend/core/auth.py, apps/backend/tests/test_auth.py | PHASE1-WEEK2-007: core/storage.py MinIO client wrapper |
| 2026-06-18 | PHASE1-WEEK2-007: Created `core/storage.py` — module-level `minio_client = Minio(f"{settings.MINIO_HOST}:{settings.MINIO_PORT}", access_key=settings.MINIO_ROOT_USER, secret_key=settings.MINIO_ROOT_PASSWORD, secure=settings.MINIO_USE_SSL)` built once at import time, plus thin wrappers `upload_file`/`download_file`/`get_presigned_url`/`delete_file` calling `put_object`/`get_object`/`presigned_get_object`/`remove_object` respectively (verified exact signatures against installed `minio==7.2.7` via `inspect.signature` before writing — `presigned_get_object` takes `expires: timedelta`, not raw seconds, so `get_presigned_url` converts `expires_seconds` via `timedelta(seconds=...)`; `get_object` returns a `BaseHTTPResponse` that must be `.read()` then `.close()` + `.release_conn()`'d, done in a `try/finally`). No bucket names hardcoded in the module — callers pass one of the four `Settings` bucket constants. Added 4 unit tests in `tests/test_storage.py`, all mocking `core.storage.minio_client` via `unittest.mock.patch` (no live MinIO needed) — full suite now 14/14 passing. `ruff check .` clean; `ruff format --check core/storage.py tests/test_storage.py` clean (had one line-length wrap needed on the `get_presigned_url` return statement, fixed immediately). Pre-commit hook passed automatically. Pushed branch; PR not opened (manual creation per established workflow). **Gotcha repeated:** ran the `minio` signature-inspection command with a bare `cd apps/backend && ...` (no subshell), which leaked cwd forward and broke the next `Write`'s PostToolUse lint hook (`lint.py` not found relative to the leaked cwd) — confirmed file content was still correct via `Read`, then ran a plain `cd` back to repo root to recover; reinforces the standing rule to always wrap `cd`-then-run in `(cd dir && cmd)` | apps/backend/core/storage.py, apps/backend/tests/test_storage.py | PHASE1-WEEK2-008: core/celery_app.py Celery app + queue routing |
| 2026-06-18 | PHASE1-WEEK2-008: Created `core/celery_app.py` — module-level `celery = Celery("fts", broker=settings.CELERY_BROKER_URL, backend=settings.CELERY_RESULT_BACKEND)` (verified against installed `celery==5.4.0`), `celery.conf.update(task_serializer="json", result_serializer="json", accept_content=["json"], timezone="UTC", enable_utc=True, task_routes={"tasks.training_tasks.*": {"queue": "training"}, "tasks.export_tasks.*": {"queue": "export"}}, task_default_queue="default")` — matches the `--queues=training,export,default` flag already in `docker-compose.yml`'s `celery_worker` command without changing it. Added 4 config-only unit tests in `tests/test_celery_app.py` (app name, broker/backend resolve from settings, serialization defaults, task_routes mapping) — full suite now 18/18 passing. `ruff check .` and `ruff format --check core/celery_app.py tests/test_celery_app.py` both clean. **Real bug found + fixed (required by this task's acceptance criteria to verify containers recover):** brought up `celery_worker`/`celery_beat` via `docker compose up -d` and found `celery_beat` still restart-looping after adding `celery_app.py` — root cause was `core/config.py`'s `REPO_ROOT_ENV_FILE = Path(__file__).resolve().parents[3] / ".env"` raising `IndexError` inside the container, since `core/config.py` resolves to `/app/core/config.py` there (only 2 parent levels above `/`) vs `apps/backend/core/config.py` locally (4 levels to repo root) — this pre-existing PHASE1-WEEK2-001 bug, not the missing module, was the actual restart-loop cause all along. Fixed with a length guard: `_CONFIG_PARENTS = Path(__file__).resolve().parents; REPO_ROOT_ENV_FILE = _CONFIG_PARENTS[3] / ".env" if len(_CONFIG_PARENTS) > 3 else None` — safe because containers already get every var via docker-compose's `env_file: .env` directive injected straight into the container env, so the in-container `.env` *file* lookup is redundant and just needs to not crash. Verified locally (18/18 tests still pass) and in Docker (`docker compose restart celery_worker celery_beat` → both `Up` after 15s+, not `Restarting`; worker log shows clean `Connected to redis://...` + `celery@... ready`). Also gitignored `celerybeat-schedule` (celery beat's runtime sqlite artifact written to `apps/backend/` on startup). Two commits pushed: the celery_app feature commit + a separate `fix(config): ...` commit for the guard. Pushed `feat/PHASE1-WEEK2-008-celery-app`; PR not opened (manual creation per established workflow). **Gotcha repeated a fourth time:** a bare `cd apps/backend && uv run python -c "import celery; ..."` (Bash tool, no subshell parens) leaked cwd forward exactly as before — caught immediately via `pwd` before the next Write and recovered with a plain `cd` back to repo root; treat `(cd dir && cmd)` as fully non-negotiable even for one-line version checks | apps/backend/core/celery_app.py, apps/backend/tests/test_celery_app.py, apps/backend/core/config.py (IndexError guard), .gitignore (celerybeat-schedule) | PHASE1-WEEK2-009: core/exceptions.py custom exception hierarchy |
| 2026-06-18 | PHASE1-WEEK2-009: Created `core/exceptions.py` — framework-agnostic `AppException(Exception)` base carrying `status_code: int` and `detail: str`, set via `__init__(detail: str | None = None) -> None` (falls back to the class-level default `detail` when not overridden, so subclasses just declare class attributes). Five concrete subclasses with sensible defaults: `NotFoundError` (404), `UnauthorizedError` (401), `ForbiddenError` (403), `ConflictError` (409), `ValidationError` (422). Deliberately no FastAPI import anywhere — keeps the hierarchy raisable from `core/`, `services/`, or `training_engine/` without pulling in the web framework; translation to `HTTPException` is deferred to routes/middleware at the API boundary (not built yet, lands in PHASE1-WEEK2-010+). Added 15 parametrized unit tests in `tests/test_exceptions.py` (default `status_code` per subclass, `detail` override via constructor, `isinstance` check against `AppException`) — full suite now 33/33 passing. `ruff check .` clean; `ruff format --check core/exceptions.py tests/test_exceptions.py` clean (PostToolUse hook's auto-formatter ran on `Write` with no manual fixes needed). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK2-009-exceptions`; PR not opened (manual creation per established workflow). **No new gotcha this session** — followed the standing `(cd dir && cmd)` subshell rule from the start, no cwd leakage | apps/backend/core/exceptions.py, apps/backend/tests/test_exceptions.py | PHASE1-WEEK2-010: main.py FastAPI app factory + router registration + CORS |
| 2026-06-18 | PHASE1-WEEK2-010: Turned `main.py` into a `create_app() -> FastAPI` factory (module-level `app = create_app()` retained so `tests/conftest.py`'s `from main import app` keeps working). Migrated the deprecated `@app.on_event("startup")` to an `@asynccontextmanager async def lifespan(app)` passed as `FastAPI(..., lifespan=lifespan)` — decided to do this in-task since it's the same file/same restructuring and it was producing a pytest deprecation warning noted back in PHASE1-WEEK2-009; not scope creep, just cleaning up the file this task already owns. Added `CORS_ORIGINS: list[str]` to `core/config.py`'s `Settings` (default `["http://localhost:3000", "http://127.0.0.1:3000"]` — pydantic-settings parses list-typed env vars as JSON automatically, consistent with the existing pattern, no new parsing code needed) and pointed `CORSMiddleware(allow_origins=settings.CORS_ORIGINS, ...)` at it instead of a hardcoded list. Added `api/v1/api_router = APIRouter()` in `apps/backend/api/v1/__init__.py` as the router-registration scaffold — left empty with a commented example (`from api.v1.routes import auth; api_router.include_router(auth.router, ...)`) since no route modules exist yet; `main.py` wires it via `app.include_router(api_router, prefix="/api/v1")` per CLAUDE.md section 5. `/health` endpoint kept exactly as-is (full spec version deferred to PHASE1-WEEK2-011 per existing backlog). Full 33-test suite passes with zero warnings (deprecation warning gone). `ruff check .` clean repo-wide; `ruff format --check` clean on all three touched files (`main.py`, `core/config.py`, `api/v1/__init__.py`) — confirmed the other 16 pre-existing-drift files noted in prior sessions are still untouched/unrelated. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK2-010-fastapi-app`; PR not opened (manual creation per established workflow). **Gotcha repeated yet again, new flavor:** this time it wasn't a `cd dir && cmd` during verification — it was a bare `cd .claude/tasks && ls -la` (no subshell) run *after* the feature work was already committed/pushed, while updating tracking files; it leaked cwd into `.claude/tasks` and broke the next `Edit`'s PostToolUse lint hook (looked for `.claude/tasks/.claude/hooks/lint.py`). Recovered with a plain `cd` back to repo root. **Updated standing rule: wrap every bare `cd <dir> && cmd` in `(parens)` for ANY directory navigation in the Bash tool, not just `apps/backend` during verification — including quick `ls`/inspection commands on tracking files.** | apps/backend/main.py, apps/backend/core/config.py, apps/backend/api/v1/__init__.py | PHASE1-WEEK2-011: GET /health returning database + redis + storage status |
| 2026-06-18 | PHASE1-WEEK2-011: Replaced `/health`'s static body with three isolated dependency checks in `main.py` — `_check_database` (`SELECT 1` via `AsyncSessionLocal`), `_check_redis` (`PING` via `redis.asyncio.from_url(settings.REDIS_URL)`, `redis==5.0.4` already in requirements.txt from Week 1 so no new dependency), `_check_storage` (`asyncio.to_thread(minio_client.bucket_exists, settings.BUCKET_DATASETS)` since the minio SDK is sync-only). Each returns `"up"`/`"down"` and is wrapped in its own try/except so one failure can't mask or crash the others; response body now exposes `database`/`redis`/`storage` plus overall `status: "healthy"|"degraded"`. **Real bug found via live verification, not just unit tests:** brought up the actual `fts_postgres`/`fts_redis`/`fts_minio` containers and ran the app locally against host-mapped ports (`localhost:5433`/`6380`/`9000`, since `.env`'s `DATABASE_URL`/`REDIS_URL`/`MINIO_HOST` use Docker-internal hostnames not resolvable from the host) — confirmed all-up → `"healthy"`. Then stopped MinIO and pointed `MINIO_HOST` at an unreachable address to simulate real outage: `minio_client.bucket_exists` took **~20.5s** to raise `MaxRetryError` (urllib3's default retry/backoff), which would make `/health` hang that long on every poll during a real outage — bad for an endpoint orchestrators/load balancers call frequently with short timeouts. Fixed by adding `_check_with_timeout` wrapping each check in `asyncio.wait_for(..., timeout=_HEALTH_CHECK_TIMEOUT_SECONDS=5.0)`; re-verified end-to-end against the same unreachable-MinIO scenario — endpoint now returns in ~6s with `storage: "down"`, `status: "degraded"`, `database`/`redis` still `"up"`. (`fts_backend`'s own Docker container couldn't be used for this verification — its `start.sh` has a pre-existing CRLF/`set -euo pipefail` parsing bug unrelated to this task, noted here but not fixed since out of scope.) Rewrote `tests/test_health.py` (7 tests: 200 status, all-up body, degraded-when-one-down, one failure-path test per dependency mocking `AsyncSessionLocal`/`redis_asyncio.from_url`/`minio_client.bucket_exists` to raise) — full suite 37/37 passing. One `test_auth.py` tampered-token test failed once mid-session; confirmed flaky/pre-existing (passed in isolation and on full-suite rerun), not a regression from this change. `ruff check .` clean repo-wide; `ruff format --check` clean on both touched files — same 14 pre-existing-drift files from prior sessions remain, untouched. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK2-011-health-endpoint`; PR not opened (manual creation per established workflow). **Gotcha, new flavor:** the PostToolUse lint/format hook's auto-fix silently stripped an added `Callable, Coroutine` import as "unused" between two separate `Edit` calls (same root cause as the PHASE1-WEEK2-006 gotcha: import added in one `Edit`, its usage added in a later one) — recovered by re-adding the import in the same `Edit` as its first usage. Also stopped/restarted the `fts_minio` container for verification and confirmed via `docker compose ps -a` that all containers were restored to their pre-session state before finishing | apps/backend/main.py, apps/backend/tests/test_health.py | PHASE1-WEEK2-012: Auth routes — POST /register, /login, /refresh, GET /me |
| 2026-06-18 | PHASE1-WEEK2-012: Created `schemas/auth.py` (`UserRegister`, `UserLogin`, `TokenResponse`, `RefreshRequest`, `UserResponse` with `ConfigDict(from_attributes=True)`) and `api/v1/routes/auth.py` wiring `POST /register` (409 `ConflictError` via `core/exceptions.py` on duplicate email, `hash_password`, creates `User`), `POST /login` (401 `UnauthorizedError` on bad credentials via `verify_password`, issues an access+refresh `TokenResponse`), `POST /refresh` (decodes via `core.auth.decode_token`, re-checks the user still exists in DB, issues a fresh token pair), `GET /me` (`Depends(get_current_user)`). Confirmed scope with the user on `POST /logout` (listed in CLAUDE.md but not in this task's original acceptance criteria) — added it as a stateless 204 no-op requiring a valid token, since this is pure JWT with no blocklist. Wired `auth.router` into `api/v1/api_router` (replacing the commented placeholder from PHASE1-WEEK2-010). **Necessary fix discovered, not pre-existing:** `core/exceptions.py`'s `AppException` hierarchy had never actually been connected to FastAPI — there was no exception handler, so raising `ConflictError`/`UnauthorizedError` from a route would have produced an unhandled 500, not the declared status code. Added `@app.exception_handler(AppException)` → `JSONResponse(status_code=exc.status_code, content={"detail": exc.detail})` in `main.py`. Added 13 tests in `tests/test_auth_routes.py` that hit the routes for real against the live dev Postgres (deliberately no DB mocking, to actually exercise the uniqueness check / password verification / token round-trip) — dogfoods `POST /register` for test setup and cleans up created rows via sync `psycopg2` (already a dependency for Alembic) so the async engine is never touched from a second path. **Real structural bug found and fixed, not just an auth-route issue:** `tests/conftest.py`'s `client` fixture was function-scoped, so every test function got its own fresh `TestClient` → its own asyncio event loop, while `core/database.py`'s async engine/connection pool is a single process-wide object — a connection opened in one test's loop would get reused or closed from a different test's (already-dead) loop, crashing with `AttributeError: 'NoneType' object has no attribute 'send'` / `RuntimeError: Event loop is closed` (Windows + asyncpg). Fixed by making the `client` fixture session-scoped — one event loop for the whole pytest run. This wasn't a workaround specific to this task's tests; it fixes the fixture itself for any future DB-touching test. Verified locally by overriding `DATABASE_URL`/`DATABASE_URL_SYNC` to `localhost:5433` (docker-mapped Postgres port) since `.env`'s default points at the Docker-internal hostname `postgres:5432`, unreachable from the host — this exactly matches what `.github/workflows/ci.yml`'s `backend-test` job already does against its own ephemeral Postgres service, so no code changes were needed to support CI, only env vars at local invocation time. Full suite 47/47 passing. `ruff check .` clean repo-wide; `ruff format --check` clean on every touched/new file — same pre-existing-drift files from prior sessions remain, untouched. Pre-commit hook passed automatically on both commits. Pushed `feat/PHASE1-WEEK2-012-auth-routes` (2 commits); PR not opened (manual creation per established workflow) | apps/backend/schemas/auth.py, apps/backend/api/v1/routes/auth.py, apps/backend/api/v1/__init__.py, apps/backend/main.py, apps/backend/tests/conftest.py, apps/backend/tests/test_auth_routes.py | PHASE1-WEEK2-013: scripts/seed_data.py — model catalog + GPU pricing |
| 2026-06-18 | PHASE1-WEEK2-013: Confirmed scope with the user before writing any code (CURRENT_TASK.md flagged the ambiguity directly): chose new DB-backed `model_catalog`/`gpu_pricing` tables over static Python config, since the planned `GET /models/catalog`/`GET /gpu/pricing` routes imply queryable data. Added `models/model_catalog.py` (`ModelCatalog`: unique/indexed `model_id`, `display_name`, `family`, `parameter_count_b` Numeric(10,2), `supports_instruct`) and `models/gpu_pricing.py` (`GpuPricing`: composite unique index on `(vendor, gpu_type)`, `vram_gb`, `price_per_hour_usd` Numeric(10,4)) — both global reference tables, no `user_id` FK. Registered in `models/__init__.py` and `migrations/env.py`'s explicit import list; autogenerated+applied migration `5d716f49b7ac`, verified `downgrade -1`→`upgrade head` round-trip and inspected the live schema via `psql \d`. Wrote `scripts/seed_data.py` — idempotent `postgresql.insert(...).on_conflict_do_update(index_elements=[...])` upserts keyed on `model_id`/`(vendor, gpu_type)`; seeds all 13 models from CLAUDE.md's catalog (CURRENT_TASK.md's draft said "12" but the real list has 13 — went with the source of truth) and 10 GPU pricing rows across AWS/GCP/Azure/RunPod/Lambda Labs; `structlog`, no `print()`. Needed a `sys.path.insert(0, parent)` bootstrap at the top of the script (`uv run python scripts/seed_data.py` sets `sys.path[0]` to the script's own dir, not cwd) plus a new `"scripts/*" = [..., "E402"]` entry in `ruff.toml` — mirrors why alembic.ini already has `prepend_sys_path = .`. Verified the script runs and re-runs idempotently against live dev Postgres (13/10 rows both times via `psql count(*)`). **Real test-writing detour:** the obvious approach (call the async seed functions directly inside `asyncio.run()`) repeatedly crashed with cross-loop asyncpg errors even after consolidating to one `asyncio.run()` and adding `engine.dispose()` — root cause is `conftest.py`'s session-scoped `client` fixture holding one event loop open for the whole test session, so any second ad-hoc loop touching the same process-wide engine/pool eventually conflicts with it. Fixed by having `tests/test_seed_data.py` invoke the script via `subprocess.run([sys.executable, "scripts/seed_data.py"], cwd=apps/backend)` (its own fresh engine/process, run twice to prove idempotency) and verifying rows with sync `psycopg2` — same pattern `test_auth_routes.py` already uses for cleanup. Also hit the now-familiar PostToolUse-hook-strips-import-as-unused gotcha once more (added `engine` to an import in one `Edit`, its usage in a separate later `Edit`) before settling on the subprocess approach, which doesn't need that import at all. Full suite 50/50 passing; `ruff check .` clean repo-wide; `ruff format --check` clean on all 7 touched/new files. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK2-013-seed-data`; PR not opened (manual creation per established workflow). **Week 2 backlog fully closed (PHASE1-WEEK2-001 through 013 all done) — Week 3 begins next** | apps/backend/models/model_catalog.py, apps/backend/models/gpu_pricing.py, apps/backend/models/__init__.py, apps/backend/migrations/env.py, apps/backend/migrations/versions/5d716f49b7ac_add_model_catalog_and_gpu_pricing_tables.py, apps/backend/scripts/seed_data.py, apps/backend/tests/test_seed_data.py, ruff.toml | PHASE1-WEEK3-001: Dataset ORM service + upload endpoint |
| 2026-06-18 | PHASE1-WEEK3-001: Built the dataset upload flow — `schemas/dataset.py` (`DatasetResponse`, `from_attributes=True`); `services/dataset_service.py` validates filename extension (`.json`/`.jsonl`) + content-type whitelist (`application/json`/`text/plain`/`application/octet-stream`) + `settings.MAX_UPLOAD_SIZE_MB` before calling `core/storage.upload_file` (MinIO, `settings.BUCKET_DATASETS`, object key `{user_id}/{uuid4()}_{filename}`), counts rows (`.jsonl`: non-blank line count; `.json`: array length or 1 for a single object), and creates the `Dataset` row with `format="unknown"` since format detection is the next task (PHASE1-WEEK3-002) and explicitly out of scope here. `api/v1/routes/datasets.py` — `POST /datasets/upload` (multipart, 201) and `GET /datasets` (current user's datasets, newest first), both behind `Depends(get_current_user)`; wired into `api/v1/api_router`. **Real bug caught by the test run, not by inspection:** the first draft read the upload into `contents = await file.read()` (needed for size/validation/row-count) and then passed the now-EOF `file.file` straight to `upload_file` — MinIO would have received 0 bytes. Fixed by uploading `BytesIO(contents)` instead. Added 5 tests in `tests/test_dataset_routes.py` (successful upload, oversized via `monkeypatch.setattr(settings, "MAX_UPLOAD_SIZE_MB", 0)`, wrong content-type, unauthenticated 401, list scoped to current user only) — all mock `services.dataset_service.upload_file` rather than hitting real MinIO, because **`.github/workflows/ci.yml`'s `backend-test` job has no MinIO service container** (only Postgres + Redis) — a real-MinIO integration test would pass locally but fail CI; this is now a standing rule (see ARCHITECTURE DECISIONS). DB assertions hit the real dev Postgres exactly like `test_auth_routes.py`. Full suite 55/55 passing; `ruff check .` clean repo-wide; `ruff format --check` clean on every touched/new file (the 6 files ruff flagged — `api/__init__.py`, `api/v1/routes/__init__.py`, `api/v1/routes/auth.py`, `schemas/__init__.py`, `schemas/auth.py`, `services/__init__.py` — are the same pre-existing-drift files noted across prior sessions, confirmed untouched this session). Hit the now-familiar PostToolUse-hook-strips-import-as-unused gotcha yet again (added `BytesIO` import in one `Edit`, its usage in a separate later `Edit`, hook auto-fixed it away in between) — recovered by re-adding both together in one `Edit`. Also leaked cwd into `apps/backend` once via a bare `cd` during verification (broke the lint hook's relative path resolution) — recovered with a plain `cd` back to repo root. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-001-dataset-upload`; PR not opened (manual creation per established workflow) | apps/backend/schemas/dataset.py, apps/backend/services/dataset_service.py, apps/backend/api/v1/routes/datasets.py, apps/backend/api/v1/__init__.py, apps/backend/tests/test_dataset_routes.py | PHASE1-WEEK3-002: training_engine/datasets/formatter.py — Alpaca/ShareGPT/ChatML |
| 2026-06-18 | PHASE1-WEEK3-002: Built `training_engine/datasets/formatter.py` — `detect_format(row) -> str` recognizing the three exact CLAUDE.md shapes (alpaca/sharegpt/chatml), raising a local `DatasetFormatError(ValueError)` (no dependency on `apps/backend/core` — confirmed nothing in the repo wires `training_engine` to import backend code, consistent with the standalone-process architecture); `to_chatml(row, format=None) -> dict` normalizes any of the three to canonical `{"messages": [{"role","content"}, ...]}` (chosen since that's what `trl.SFTTrainer`/`DPOTrainer` expect), auto-detecting format when not given. 17 new tests in `training_engine/tests/test_formatter.py`. **Two real bugs found via verification, not inspection:** (1) `pytest` was never actually installed in `training_engine/.venv` nor listed in its `requirements.txt`, despite the root `Makefile` already invoking `pytest tests/` against it — added `pytest==8.2.0` to `requirements.txt` + new `requirements-dev.txt` (mirrors `apps/backend`'s freeze-vs-curated-dev-list split). (2) First test run failed with `ModuleNotFoundError: No module named 'datasets.formatter'` — root cause: the empty, dead `training_engine/__init__.py` (nothing imports `training_engine.*` anywhere, confirmed via repo-wide grep) made pytest's rootdir package-walk treat `training_engine` as a subpackage and continue one level further up to the monorepo root for its `sys.path` insertion, so `from datasets.formatter import ...` fell through to the pip-installed HuggingFace `datasets==2.19.0` library (already in `requirements.txt`) instead of the local package. Fixed by deleting `training_engine/__init__.py`. **Flagged but not fixed (out of scope):** this surfaces a deeper unresolved naming collision — `training_engine/datasets/` as a local package name will collide with the pip `datasets` (HuggingFace) library the moment a future module there (e.g. planned `loader.py`) needs `import datasets` for `load_dataset()`; needs a deliberate decision before PHASE1-WEEK3-003 or later HF-loading work (see ARCHITECTURE DECISIONS). Full suite 17/17 passing; `ruff check .` clean; `ruff format --check` clean on all touched/new files (confirmed via `git status` that the 6 ruff-flagged pre-existing `__init__.py` stub files across `training_engine/*/` are untouched, same pre-existing-drift pattern as `apps/backend`). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-002-dataset-formatter`; PR not opened (manual creation per established workflow) | training_engine/datasets/formatter.py, training_engine/tests/__init__.py, training_engine/tests/test_formatter.py, training_engine/requirements.txt, training_engine/requirements-dev.txt, training_engine/__init__.py (deleted) | PHASE1-WEEK3-003: training_engine/datasets/quality_check.py — dedup, stats, language |
| 2026-06-18 | PHASE1-WEEK3-003: Built `training_engine/datasets/quality_check.py` — `run_quality_check(rows: list[dict]) -> dict` over canonical ChatML rows. CURRENT_TASK.md's draft acceptance criteria deliberately left three scope decisions open for confirmation; made and documented all three rather than pausing (instructed this session to execute the task completely): exact-match dedup only (lowercased/whitespace-collapsed concatenation of message contents as the dedup key; no fuzzy matching — real scope increase, no spec for it), word-count-only stats (`min`/`max`/`mean`/`median` via plain `.split()`, no tokenizer dependency), and `langdetect==1.0.9` for language detection (new pinned dependency, pure-Python, no native/ML deps; `DetectorFactory.seed = 0` set at import time for deterministic results — see ARCHITECTURE DECISIONS for the gotcha this avoids). Report shape: `{total_rows, duplicate_rows, unique_rows, duplicate_indices, word_count: {...}, languages: {<code>: count}}`; blank rows are skipped in language detection (would raise `LangDetectException`) but still counted in `total_rows`. **Resolved the standing `datasets`-naming-collision landmine for this task specifically:** `quality_check.py` never needs `import datasets` (the HF library) since it only operates on already-loaded dicts — confirmed the collision genuinely doesn't trigger here; it remains open only for the still-unbuilt `loader.py` (see ARCHITECTURE DECISIONS, updated). 10 new tests in `training_engine/tests/test_quality_check.py`, using long realistic English/French sentences as fixtures (langdetect is documented unreliable on short strings — verified determinism via direct REPL checks before locking expected language codes into assertions). Full suite 27/27 passing (17 existing + 10 new); `uv run ruff check .` clean; `uv run ruff format --check` clean on both new files (confirmed via `git status` that the same 3 pre-existing-drift files flagged in PHASE1-WEEK3-002 — `datasets/__init__.py`, `datasets/formatter.py`, `tests/test_formatter.py` — remain untouched). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-003-quality-check`; PR not opened (manual creation per established workflow) | training_engine/datasets/quality_check.py, training_engine/tests/test_quality_check.py, training_engine/requirements.txt (added langdetect==1.0.9) | PHASE1-WEEK3-004: POST /datasets/{id}/format + /quality-check endpoints |
| 2026-06-18 | PHASE1-WEEK3-004: Asked the user to resolve the genuine architectural fork flagged in CURRENT_TASK.md's blocker before writing any code — how should `apps/backend` call `training_engine`'s `formatter.to_chatml`/`detect_format` and `quality_check.run_quality_check`, given the two are separate standalone processes/environments. User chose: **port the logic into `apps/backend/services/`** over installing `training_engine` as a backend dependency or dispatching via Celery (see ARCHITECTURE DECISIONS for the now-standing pattern this establishes). Created `services/dataset_format.py` and `services/dataset_quality.py` as near-verbatim ports of `training_engine/datasets/formatter.py`/`quality_check.py`; added `langdetect==1.0.9` to `apps/backend/requirements.txt` (intentional duplicate pin alongside `training_engine/requirements.txt`, installed via `uv pip install`). Extended `services/dataset_service.py`: `get_dataset(dataset_id, user, db)` (404 `NotFoundError` if missing/not owned), `_parse_rows(contents, filename)` (jsonl → one dict per non-blank line, json → the array or `[the single object]`; refactored `_count_rows` to call `len(_parse_rows(...))` instead of duplicating the parsing), `format_dataset` (downloads raw bytes via `core.storage.download_file`, parses rows, runs `detect_format` on the first row, persists to `Dataset.format`, maps `DatasetFormatError` → 422 `ValidationError`), `quality_check_dataset` (downloads + parses rows, normalizes every row via `to_chatml` — passing the dataset's already-detected format when not `"unknown"`, auto-detecting otherwise — runs `run_quality_check`, persists the report to `Dataset.quality_report`). Wired `POST /datasets/{id}/format` and `POST /datasets/{id}/quality-check` in `api/v1/routes/datasets.py`, both `Depends(get_current_user)`, both returning the updated `DatasetResponse` (chose this response shape for consistency between the two new endpoints over a bare report body for quality-check). Added a `mock_minio_download` fixture (keyed by `storage_path`, mirrors the existing `mock_minio_upload` pattern — no live MinIO, per the standing CI rule) and an `_upload_dataset` helper plus `EN_SENTENCE`/`_alpaca_jsonl` fixtures (borrowed the long deterministic English sentence from `training_engine/tests/test_quality_check.py` so language detection doesn't flake) to `tests/test_dataset_routes.py`; 13 new tests covering format-detection success, unrecognized-row 422, 404 on missing/not-owned dataset for both endpoints, 401 unauthenticated for both, quality-check persisting a report with correct `total_rows`/`languages`, and quality-check reusing a previously-detected format. Full suite 64/64 passing; `ruff check .` clean repo-wide; `ruff format --check` clean on all 6 touched/new files (same 14 pre-existing-drift files from prior sessions remain, confirmed untouched). Hit the now-familiar PostToolUse-hook-strips-import-as-unused gotcha once during this session (an `Edit` adding only new imports with no usage yet got reformatted/stripped) — recovered by redoing the edit with imports and their first usage together in one call. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-004-dataset-endpoints`; PR not opened (manual creation per established workflow) | apps/backend/services/dataset_format.py, apps/backend/services/dataset_quality.py, apps/backend/services/dataset_service.py, apps/backend/api/v1/routes/datasets.py, apps/backend/tests/test_dataset_routes.py, apps/backend/requirements.txt (added langdetect==1.0.9) | PHASE1-WEEK3-005: Fine-tune job creation endpoint + config validation |
| 2026-06-19 | PHASE1-WEEK3-005: Asked the user up front to pick the training_config validation strictness (the one open scope question CURRENT_TASK.md flagged) — offered minimal per-method required keys / generic-only / strict full schema per methodology; user picked **minimal per-method required keys**. `schemas/job.py` — `FineTuneJobCreate` (base_model_id, methodology, training_config dict, optional dataset_id/gpu_type/cloud_vendor), `FineTuneJobResponse` (`from_attributes=True`, mirrors every `FineTuneJob` ORM column), `FineTuneJobConfigResponse` (bare `training_config` dict, backs `GET /jobs/{id}/config`). `services/job_service.py` — `SUPPORTED_METHODOLOGIES` (the six CLAUDE.md section 7 values), `COMMON_REQUIRED_KEYS = {learning_rate, num_epochs, batch_size}` applied to every methodology, `METHOD_SPECIFIC_REQUIRED_KEYS` layering `{lora_r, lora_alpha}` on lora/qlora, `{beta}` on dpo/orpo, `{reward_model_id}` on rlhf (sft needs nothing beyond the common set); both an unsupported methodology and any missing required key raise `core.exceptions.ValidationError` → 422 through the existing `AppException` handler (no new wiring needed — that handler's been live since PHASE1-WEEK2-012). `create_job` calls `dataset_service.get_dataset(dataset_id, user, db)` verbatim when `dataset_id` is provided — reusing PHASE1-WEEK3-004's exact 404-if-missing-or-not-owned pattern instead of duplicating the ownership check; new jobs persist with `status="pending"`. Added `list_jobs`/`get_job`, both scoped to `FineTuneJob.user_id == user.id`. Wired `api/v1/routes/jobs.py` (`POST /jobs` 201, `GET /jobs`, `GET /jobs/{job_id}`, `GET /jobs/{job_id}/config`), all `Depends(get_current_user)`; registered in `api/v1/__init__.py` alongside the existing `auth`/`datasets` routers. 16 new tests in `tests/test_job_routes.py`, following the exact `_register`/`_auth_headers`/`emails_to_cleanup` patterns from `test_dataset_routes.py` (own local copies since those fixtures aren't in shared `conftest.py`) — covers create success with/without `dataset_id`, invalid methodology, missing common keys, missing method-specific keys, nonexistent and other-user's-dataset `dataset_id` → 404, unauthenticated → 401, list scoped to current user only, get/get-config success + 404 + cross-user 404 + 401. Full suite 80/80 passing (64 prior + 16 new); `ruff check .` clean repo-wide; `ruff format --check` flagged the same pre-existing-drift files noted across every prior session (confirmed via `git status --short` that none of the 5 touched/new files for this task were among them). Pre-commit hook passed automatically on commit. Pushed `feat/PHASE1-WEEK3-005-job-creation`; PR not opened (manual creation per established workflow). **Week 3 backend scope (PHASE1-WEEK3-001 through 005) is now fully closed — remaining Week 3 tasks (006+) are all frontend** | apps/backend/schemas/job.py, apps/backend/services/job_service.py, apps/backend/api/v1/routes/jobs.py, apps/backend/api/v1/__init__.py, apps/backend/tests/test_job_routes.py | PHASE1-WEEK3-006: Frontend Next.js root layout + sidebar + header |
| 2026-06-19 | PHASE1-WEEK3-006: Confirmed the one open scope question with the user first (CURRENT_TASK.md flagged it: CLAUDE.md lists `Breadcrumb.tsx`/`PageContainer.tsx` in `components/layout/` but no page exists yet to use either) — user picked **build `PageContainer` now, defer `Breadcrumb`**. Read `apps/frontend/AGENTS.md`'s warning (surfaced via `apps/frontend/CLAUDE.md`'s `@AGENTS.md` import) that this is Next.js 16 with breaking changes from training data, and checked the bundled `node_modules/next/dist/docs/` (layouts-and-pages, project-structure, link) before writing code — confirmed `usePathname`/`<Link>`/route-groups all work as expected (see ARCHITECTURE DECISIONS for the new `LayoutProps`/`PageProps` helper-type note). Created `app/(dashboard)/layout.tsx` (`Sidebar` + `Header` wrapping `children`) and `app/(dashboard)/page.tsx` (placeholder); `components/layout/Sidebar.tsx` (`"use client"` for `usePathname`-driven active-link state — 12 nav items covering every CLAUDE.md module route + dashboard home, each with a lucide icon, active state via `aria-current="page"` + `bg-secondary`/`text-secondary-foreground`); `components/layout/Header.tsx` (kept as a server component — interactivity is already isolated inside `ui/dropdown-menu.tsx`'s own `"use client"` — placeholder account dropdown, all items `disabled` since real auth lands in PHASE1-WEEK3-009); `components/layout/PageContainer.tsx` (max-width/padding wrapper). **Real, pre-existing bug found and fixed (not scope creep — directly blocked correct rendering of what this task builds):** `globals.css` was missing the shadcn neutral theme CSS variable set entirely (only `--background`/`--foreground` existed) despite `components.json` declaring `cssVariables: true` — every `ui/*` component built in Week 1 (Button, Avatar, DropdownMenu, Separator, ScrollArea, ...) referenced `border-border`/`bg-muted`/`bg-primary`/etc. with no backing CSS custom property, so they'd have rendered fully unstyled the first time any page actually used them. Added the full standard token set (`--card`, `--popover`, `--primary`, `--secondary`, `--muted`, `--accent`, `--destructive`, `--border`, `--input`, `--ring`, `--radius-sm/md/lg/xl`) to `:root` + the existing `prefers-color-scheme: dark` media query (kept that pattern rather than introducing next-themes' `.dark`-class strategy, since no `ThemeProvider` exists yet) and wired them into `@theme inline`. Also deleted `app/page.tsx` (the default create-next-app scaffold, which would otherwise route-conflict with `/` now owned by `app/(dashboard)/page.tsx`) and updated `app/layout.tsx`'s `<title>`/description off the "Create Next App" default while already in that file. **Verified for real, not just `npm run build`:** ran `npm run dev`, fetched `/` over HTTP, confirmed `data-slot="sidebar"`, header text, dashboard heading, and `aria-current="page"` + `bg-secondary` active-link classes were present in the actual response HTML; then fetched the compiled CSS chunk directly and confirmed `.border-border { border-color: var(--border) }` resolves against a real emitted `--border: #e5e5e5` (light, converted from the oklch source) / `lab(...)`/`#ffffff1a` (dark) value in `:root` — not just that the utility class names existed in markup. `npm run lint` (ESLint) clean; `npm run build` (Turbopack) compiled successfully with `/` listed as a single static route (confirms no route conflict). Pre-commit hook (ESLint + TypeScript typecheck on frontend, confirmed via this session that the hook also runs `eslint --fix` on `.ts(x)` files, not just `ruff` on Python — see ARCHITECTURE DECISIONS) passed automatically. **Gotcha confirmed to extend to the frontend half of the repo:** leaked Bash cwd from a bare `cd apps/frontend && find ...` (no subshell parens) broke the lint hook's relative path resolution on the very next `Edit` — recovered with a plain `cd` back to repo root; the standing rule isn't backend-specific. Pushed `feat/PHASE1-WEEK3-006-frontend-layout`; PR not opened (manual creation per established workflow) | apps/frontend/app/(dashboard)/layout.tsx, apps/frontend/app/(dashboard)/page.tsx, apps/frontend/components/layout/Sidebar.tsx, apps/frontend/components/layout/Header.tsx, apps/frontend/components/layout/PageContainer.tsx, apps/frontend/app/globals.css, apps/frontend/app/layout.tsx, apps/frontend/app/page.tsx (deleted) | PHASE1-WEEK3-007: Frontend lib/api.ts typed axios client |
| 2026-06-19 | PHASE1-WEEK3-007: Confirmed both open scope questions with the user before coding (CURRENT_TASK.md flagged them as genuine forks, not guessed at): (1) 401 handling, given real auth (`useAuth`, token storage) doesn't land until PHASE1-WEEK3-009 — user picked **no-op for now** (pass-through response interceptor, comment marking real redirect/refresh as PHASE1-WEEK3-009 work); (2) how much of CLAUDE.md section 5's ~25-route table to cover now — user picked **base axios instance only**, no per-endpoint helpers yet (each feature task adds typed helpers to `lib/api.ts` as it lands, once PHASE1-WEEK3-008's shared types exist to type response bodies against — avoids writing helpers against untyped `any` response shapes now only to redo them next task). Built `apps/frontend/lib/api.ts`: `getAccessToken()` reads a `fts_access_token` key from `window.localStorage`, guarded with `typeof window === "undefined"` so it's safe to import from server components too — this is deliberately only the *read* hook point; the *write* (actually storing a token after login) is still TBD and lands with `useAuth` in PHASE1-WEEK3-009. `api = axios.create({ baseURL: process.env.NEXT_PUBLIC_API_URL })` — confirmed this env var already existed in `.env.example` (added Week 1: `NEXT_PUBLIC_API_URL=http://localhost:8000`), no new var invented. Request interceptor attaches `Authorization: Bearer <token>` when present; response interceptor is a bare pass-through (`(response) => response, (error) => Promise.reject(error)`). No page consumes this yet (by design — this task only builds the client itself), so verification was build/lint-level only: `npm run lint` clean, `npm run build` (Turbopack) compiled successfully with TypeScript passing and `/` still the only static route (no regression). Pre-commit hook (ESLint + TypeScript typecheck) passed automatically on commit. **No new gotcha this session** — used `(cd apps/frontend && cmd)` subshell wrapping throughout for both lint/build verification commands, no cwd leakage into the lint hook. Pushed `feat/PHASE1-WEEK3-007-api-client`; PR not opened (manual creation per established workflow) | apps/frontend/lib/api.ts | PHASE1-WEEK3-008: Frontend types/index.ts shared types |
| 2026-06-19 | PHASE1-WEEK3-008: Confirmed two real scope forks with the user before coding, both flagged in CURRENT_TASK.md: (1) hand-write vs. generate-from-OpenAPI — user picked **generate from OpenAPI**; (2) given `FineTuneJob.status` had no real backend constraint (plain `String(50)`, only `"pending"` ever set in code, despite ARCHITECTURE.md's training-flow diagram mentioning `"queued"`), whether to type the TS field as a literal union anyway — user picked **literal union of likely values** (`pending`/`queued`/`running`/`completed`/`failed`/`cancelled`), which surfaced a follow-up real decision: generating a literal union from OpenAPI requires the backend to actually constrain the field, so asked a second explicit question on whether to tighten `schemas/job.py`/`schemas/dataset.py` to `Literal[...]` — user approved. Added `Methodology`/`JobStatus` Literal aliases to `schemas/job.py` (methodology: the six CLAUDE.md section 7 values, matches `job_service.SUPPORTED_METHODOLOGIES`; status: the six-value lifecycle above) and `DatasetFormat` to `schemas/dataset.py` (matches `dataset_format.SUPPORTED_FORMATS` + `dataset_service.UNDETECTED_FORMAT`). Built `apps/backend/scripts/export_openapi.py` (mirrors `seed_data.py`'s `sys.path.insert` bootstrap; first attempt miscalculated the cross-app output path via `parents[3]` instead of `parents[2]` and wrote to a stray repo-root-level `frontend/` dir — caught immediately by inspecting the printed path, fixed, deleted the stray dir) writing `app.openapi()` to `apps/frontend/openapi/schema.json`. Added `openapi-typescript@^7.13.0` as a frontend devDependency + `npm run generate:types` script wrapping `openapi-typescript ./openapi/schema.json -o ./types/api-schema.d.ts --empty-objects-unknown` (the flag was necessary — without it, `dict[str, Any]` fields like `quality_report`/`training_config` generate as `Record<string, never>` instead of the usable `Record<string, unknown>`; found by inspecting the first-pass generated output before writing `types/index.ts` against it). `types/index.ts` re-exports `UserResponse`/`TokenResponse`/`DatasetResponse`/`FineTuneJobResponse`/`FineTuneJobConfigResponse` plus derived `DatasetFormat`/`FineTuneJobStatus`/`Methodology` literal types indexed off the response types (not hand-redeclared) so they can't drift from the generated schema. Committed both `openapi/schema.json` and `types/api-schema.d.ts` (not gitignored) since CI/build never runs the backend — same "generate once, commit, regenerate by hand on change" pattern this repo already uses for Alembic migrations (see ARCHITECTURE DECISIONS). Verified backend: full 80/80 test suite passes (run with `DATABASE_URL`/`DATABASE_URL_SYNC` overridden to `localhost:5433` per the standing local-vs-CI pattern — first run without the override hit `psycopg2.OperationalError` exactly as documented), confirming the Literal tightening didn't break any test (none assert exact `ValidationError` message text, only `status_code == 422`); `ruff check`/`ruff format --check` clean on touched files. Verified frontend: `npm run lint` clean, `npm run build` (Turbopack) compiled successfully with TypeScript passing. Pre-commit hook (ruff + ESLint + TypeScript typecheck) passed automatically. Hit the standing leaked-cwd-breaks-the-lint-hook gotcha twice this session (bare `cd apps/frontend && cmd` during `npm install`/codegen verification) — recovered both times with a plain `cd` back to repo root before the next Edit. Pushed `feat/PHASE1-WEEK3-008-shared-types`; PR not opened (manual creation per established workflow) | apps/backend/schemas/job.py, apps/backend/schemas/dataset.py, apps/backend/scripts/export_openapi.py, apps/frontend/package.json, apps/frontend/package-lock.json, apps/frontend/openapi/schema.json, apps/frontend/types/api-schema.d.ts, apps/frontend/types/index.ts | PHASE1-WEEK3-009: Frontend hooks/useAuth.ts with token refresh |
| 2026-06-19 | PHASE1-WEEK3-009: Confirmed two genuine scope forks with the user before coding, both flagged in CURRENT_TASK.md as real mismatches rather than guessed at: (1) refresh-token storage — ARCHITECTURE.md's AUTH FLOW diagram says httpOnly cookie, but `POST /api/v1/auth/refresh` (PHASE1-WEEK2-012) takes `refresh_token` in the JSON body with no cookie code anywhere in `core/auth.py`/the route, and `lib/api.ts` (PHASE1-WEEK3-007) was already built against localStorage — user picked **localStorage** (new `fts_refresh_token` key); (2) 401 interceptor behavior — user picked **silent refresh-and-retry** over a bare redirect. Built `hooks/useAuth.ts` — TanStack Query `useQuery` for `GET /auth/me` (`enabled: Boolean(getAccessToken())`, prevents firing with no token), `useMutation`s for `POST /auth/login` (stores tokens, invalidates the `me` query) and `POST /auth/register` (no auto-login, register returns no tokens), and a `logout()` wrapping `POST /auth/logout` that always clears tokens + the `me` cache in a `finally`. Rewrote `lib/api.ts`'s response interceptor: single-flight (`refreshPromise` module-level guard) silent refresh-and-retry on 401, exempting `/auth/login`/`/auth/register`/`/auth/refresh` itself from triggering it, using plain `axios.post` (not the `api` instance) for the refresh call to avoid recursing through the same interceptors, `_retry` flag to prevent infinite retry loops, falls back to `clearTokens()` + `window.location.href = "/login"` if refresh itself fails. Added `app/providers.tsx` (`QueryClientProvider`, first real wiring of the `@tanstack/react-query` dependency installed since Week 1) into `app/layout.tsx`. Extracted `Header`'s account dropdown into a new client component `components/layout/AccountMenu.tsx` wired to `useAuth()` (real `user.full_name`, working sign-out + redirect to `/login`) — `Header` itself stays a server component, continuing the PHASE1-WEEK3-006 pattern. **Verification:** confirmed via `ToolSearch` that no browser-automation tool exists in this environment, and no login page exists yet (lands next task) — so verified instead via (a) `npm run lint` + `npm run build` (Turbopack, TypeScript) both clean, (b) running the real backend locally (`uv run uvicorn`, `DATABASE_URL`/`DATABASE_URL_SYNC` overridden to `localhost:5433` per the standing pattern) and `curl`-exercising register/login/me/refresh/logout to confirm the live response shapes exactly match what the new code assumes, and (c) a **temporary** Node script (written, run, then deleted — never part of the commit) that imported the real `lib/api.ts` module via `node --experimental-strip-types` with a minimal `window.localStorage` shim, logged in for real tokens, corrupted the stored access token to simulate expiry, then called `GET /me` through the actual `api` instance and confirmed it succeeded transparently with a new, different access token afterward — proof the real shipped interceptor code (not a reimplementation) does the silent-refresh-and-retry correctly end-to-end against the live backend. This Node-script-against-the-real-module technique is now the documented fallback for verifying client-only interactive logic when no browser tool is available (see ARCHITECTURE DECISIONS). Hit the standing leaked-Bash-cwd gotcha again (a bare `cd apps/frontend && node ...`, no subshell) — caught via a `git status` showing repo-root-relative paths gone short and the temp script still present; recovered with a plain `cd` back to repo root, then deleted the temp script for real before staging. Pre-commit hook (ESLint + TypeScript typecheck) passed automatically. Pushed `feat/PHASE1-WEEK3-009-use-auth`; PR not opened (manual creation per established workflow) | apps/frontend/lib/api.ts, apps/frontend/hooks/useAuth.ts, apps/frontend/app/providers.tsx, apps/frontend/app/layout.tsx, apps/frontend/components/layout/AccountMenu.tsx, apps/frontend/components/layout/Header.tsx | PHASE1-WEEK3-010: Frontend login + register pages with Zod validation |
| 2026-06-19 | PHASE1-WEEK3-010: Confirmed two genuine scope forks with the user before coding, both flagged in CURRENT_TASK.md: (1) post-register flow, since `/auth/register` returns no tokens — user picked **auto-login** (chain `login()` with the same submitted credentials, then redirect like a normal login) over redirect-to-login-with-a-message; (2) "redirect back to the intended page" after a bounce to `/login` — user picked **build it now**, a real scope addition beyond the draft criteria. Created the `(auth)` route group: `app/(auth)/layout.tsx` (minimal centered layout, no Sidebar/Header), `app/(auth)/login/page.tsx` (RHF + Zod, calls `useAuth().login`, surfaces the backend's 401 `detail` string via `axios.isAxiosError`), `app/(auth)/register/page.tsx` (RHF + Zod schema mirroring backend's `UserRegister` exactly — `password.min(8)` — calls `useAuth().register` then immediately `useAuth().login` with the same credentials). Implemented the `next`-redirect end-to-end: `lib/api.ts`'s 401-refresh-failure fallback now builds `/login?next=<encoded current path+search>` (skipping the param when already on `/login`); both pages read `next` via `useSearchParams` (default `/`), `router.push(next)` on success, and propagate it through the login↔register cross-links. Each `useSearchParams`-consuming component is wrapped in `<Suspense>` per Next.js 16's static-prerendering rule (confirmed via `node_modules/next/dist/docs`) — verified both `/login` and `/register` still build as static routes. **Verification — first task with a real page to exercise:** `npm run lint`/`npm run build` clean; brought up `fts_postgres`/`fts_redis`/`fts_minio` (all stopped at session start) + a local `uv run uvicorn` backend + `npm run dev` frontend; fetched `/login`/`/register` over HTTP and confirmed real form markup in the server-rendered HTML; `curl`'d the exact register→login chain the register page performs (register succeeds with no tokens → login with the same credentials returns a token pair) and a wrong-password login to confirm the 401 body shape (`{"detail": "..."}`) matches `getErrorDetail`'s extraction; ran a **temporary** `node --experimental-strip-types` script (deleted before committing) importing the real `lib/api.ts` module with a `window.location`/`localStorage` shim, forced a refresh failure from `/datasets?page=2`, and confirmed the real interceptor redirects to `/login?next=%2Fdatasets%3Fpage%3D2`. The interactive click-through inside the React components still couldn't be exercised directly (no browser-automation tool, confirmed again) — covered instead by tracing the component code against the verified live API contract; stated explicitly rather than claimed as fully browser-verified. Cleaned up both test users via `psycopg2` and restored Docker/process state to pre-session (all three containers stopped again). Pre-commit hook (ESLint + TypeScript typecheck) passed automatically. Pushed `feat/PHASE1-WEEK3-010-auth-pages`; PR not opened (manual creation per established workflow) | apps/frontend/app/(auth)/layout.tsx, apps/frontend/app/(auth)/login/page.tsx, apps/frontend/app/(auth)/register/page.tsx, apps/frontend/lib/api.ts | PHASE1-WEEK3-011: Frontend Dataset Studio page — upload + format + quality report |
| 2026-06-19 | PHASE1-WEEK3-011: Confirmed the one real spec/implementation gap with the user before coding (CURRENT_TASK.md flagged it: CLAUDE.md's route table lists `GET /datasets/{id}/preview` and the directory structure implies `GET /datasets/{id}`, but neither existed in the real backend) — user picked **add `GET /datasets/{id}` only** (the `dataset_service.get_dataset` function already existed, just unexposed by any route) and **skip `/preview`/`DatasetPreview.tsx` entirely**, flagged as a backend follow-up. Backend: added the route + 4 new tests (success, 404, ownership-scoped 404, 401), regenerated `openapi/schema.json`/`types/api-schema.d.ts` via the established pipeline. Frontend: `hooks/useDatasets.ts` (list/get queries + upload/format/quality-check mutations, mirrors `useAuth.ts`'s pattern, invalidates the list cache on every mutation success); `components/dataset/{DatasetUploader,FormatSelector,QualityReport}.tsx`; three pages under `app/(dashboard)/datasets/` (list with empty state, upload, detail wiring format-detection + quality-check). Extracted `getErrorDetail` (previously copy-pasted in both `(auth)` pages) into `lib/utils.ts`, used by the new files only (didn't touch the existing auth pages, kept the diff scoped). **Discovered this codebase's shadcn setup uses base-ui's `render` prop, not `asChild`** (see ARCHITECTURE DECISIONS) — caught and fixed two `asChild`-style usages before they'd have type-errored. **Verification:** backend full suite 84/84 passing against real local Postgres/Redis; frontend `npm run lint`/`npm run build` clean (two real type errors fixed: a possibly-undefined narrowing issue, and a type-predicate needing an index signature). Live end-to-end: brought up `fts_postgres`/`fts_redis`/`fts_minio` + a local `uv run uvicorn` backend (discovered real local-env friction along the way — `MINIO_ENDPOINT` alone doesn't fix local storage connectivity, `MINIO_HOST`/`MINIO_PORT` are what's actually read; also had to kill two stale leftover `uvicorn` processes squatting on port 8000 from earlier attempts in the same session — see ARCHITECTURE DECISIONS for both), then `curl`'d the full register→login→upload→get→format→quality-check→get→list chain confirming every response shape matches the new frontend code. Fetched real server-rendered HTML for all three new routes via `npm run dev` (client data fetch shows as a loading shell in static HTML — same known no-browser-tool limitation as prior frontend tasks). Went further than the established Node-script pattern by writing one that imports the real `lib/api.ts` axios instance directly (the same instance `useDatasets.ts` uses) and drives the entire upload/format/quality-check/list flow with real assertions against the live backend — temporary script, deleted before committing, confirmed via `git status`. Cleaned up verification test users via `psycopg2` and restored all Docker containers + dev processes to their pre-session stopped state. Pre-commit hook (ruff + ESLint + TypeScript typecheck) passed automatically. Pushed `feat/PHASE1-WEEK3-011-dataset-studio`; PR not opened (manual creation per established workflow) | apps/backend/api/v1/routes/datasets.py, apps/backend/tests/test_dataset_routes.py, apps/frontend/lib/utils.ts, apps/frontend/openapi/schema.json, apps/frontend/types/api-schema.d.ts, apps/frontend/hooks/useDatasets.ts, apps/frontend/components/dataset/{DatasetUploader,FormatSelector,QualityReport}.tsx, apps/frontend/app/(dashboard)/datasets/{page.tsx,upload/page.tsx,[datasetId]/page.tsx} | PHASE1-WEEK3-012: Frontend Training Config Builder skeleton |
| 2026-06-20 | PHASE1-WEEK3-012: Asked the user to resolve three real scope forks flagged in CURRENT_TASK.md before coding: (1) no `GET /models/catalog` route existed yet — user picked **add it now** over a free-text stub; (2) whether to wire `POST /jobs` for real or stay a pure skeleton — user picked **wire it for real**; (3) how literally to build the "full parameter panel" given the backend only enforces a minimal per-methodology required-key set — user picked **broader panel with extra common ML params** (`warmup_ratio`/`weight_decay`/`max_seq_length`/`gradient_accumulation_steps` on top of the backend-enforced keys; allowed since `training_config` is a free-form dict). Backend: `schemas/model_catalog.py`, `services/model_catalog_service.py`, `api/v1/routes/models.py` (`GET /models/catalog`, `GET /models/catalog/{model_id:path}` — the `:path` converter is required since real model IDs contain `/`, see ARCHITECTURE DECISIONS), wired into `api_router`; 5 new tests inserting/cleaning up their own `model_catalog` rows via `psycopg2` since CI never runs `seed_data.py` (see ARCHITECTURE DECISIONS). Frontend: `hooks/useModelCatalog.ts`, `hooks/useJobs.ts` (mirrors `useDatasets.ts`'s pattern); `components/training/{MethodologyCard,ParameterField,JobStatusBadge,TrainingControls}.tsx`; `ConfigBuilder.tsx` (RHF + Zod — methodology grid, `Controller`-bound base-model/dataset `Select`s, common + broader numeric fields with pre-filled defaults to dodge the empty-string-coerces-to-0 footgun of optional `z.coerce.number()` fields, conditionally-rendered method-specific sections whose `superRefine` required-ness mirrors `job_service.py`'s `METHOD_SPECIFIC_REQUIRED_KEYS` exactly); `app/(dashboard)/config/page.tsx` hosts it, `app/(dashboard)/config/[jobId]/page.tsx` is a read-only detail view (`JobStatusBadge` + `TrainingControls` + pretty-printed `training_config`). Regenerated `openapi/schema.json`/`types/api-schema.d.ts`, added `ModelCatalogResponse` to `types/index.ts`. **Verification:** backend full suite 89/89 passing (confirmed the much-larger-than-before 49-file `ruff format --check` drift is entirely pre-existing via `git status` — none of this session's files are in it); frontend `npm run lint`/`npm run build` clean (`/config`, `/config/[jobId]` registered, no route conflicts). Live end-to-end: brought up all three Docker containers (stopped at session start), ran `seed_data.py` for real catalog data, started a local backend and confirmed `/health` all-up; `curl`'d `GET /models/catalog` (13 real models), `GET /models/catalog/meta-llama/Meta-Llama-3-8B` (proves the `:path` converter works against a real slash-containing ID, not a sanitized fixture), and a full `POST /jobs` using the exact training_config shape `buildTrainingConfig` produces — all matched. `npm run dev`'d and fetched both new routes' HTML (static markup present; `[jobId]` shows the known client-fetch loading-shell limitation, no browser tool available). Cleaned up the verification user via `psycopg2` (cascades to its job), killed the dev/uvicorn processes on ports 3000/8000, stopped all three Docker containers. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-012-config-builder`; PR not opened (manual creation per established workflow) | apps/backend/schemas/model_catalog.py, apps/backend/services/model_catalog_service.py, apps/backend/api/v1/routes/models.py, apps/backend/api/v1/__init__.py, apps/backend/tests/test_model_catalog_routes.py, apps/frontend/openapi/schema.json, apps/frontend/types/api-schema.d.ts, apps/frontend/types/index.ts, apps/frontend/hooks/{useModelCatalog,useJobs}.ts, apps/frontend/components/training/{MethodologyCard,ParameterField,JobStatusBadge,TrainingControls,ConfigBuilder}.tsx, apps/frontend/app/(dashboard)/config/{page.tsx,[jobId]/page.tsx} | PHASE1-WEEK3-013: Frontend ParameterTooltip component |
| 2026-06-20 | PHASE1-WEEK3-013: ParameterTooltip component — confirmed two scope forks before coding (CURRENT_TASK.md): user chose **Popover** (click-to-open) over hover Tooltip despite the CLAUDE.md name, and **richer copy** (description + typical default + why it matters) in a central `PARAMETER_INFO` map (mirrors `METHODOLOGY_INFO`). Built `components/training/ParameterTooltip.tsx` (11 entries for every ConfigBuilder field); extended `ParameterField.tsx` with optional `info?: ParameterInfo`; wired all fields in `ConfigBuilder.tsx`. Trigger: ghost `Button size="icon-xs"` + lucide `Info` via `PopoverTrigger render={<Button ... />}` + `aria-label="What is <label>?"`. Merged via PR #30 on `feat/PHASE1-WEEK3-013-parameter-tooltip`. **Re-verified this session** (tracking files were stale — CURRENT_TASK/MEMORY still pointed at 013 as next): `npm run lint` clean (pre-existing RHF `watch()` warning only); `npm run build` (Turbopack) clean; `/config` SSR HTML from existing dev server on port 3000 shows 9 `aria-label="What is ...?"` triggers for default-visible LoRA fields — popover click not browser-tested. Updated CURRENT_TASK.md → PHASE2-001, MEMORY.md phase/session log. **Week 3 and Phase 1 foundation complete.** | apps/frontend/components/training/{ParameterTooltip,ParameterField,ConfigBuilder}.tsx, .claude/tasks/CURRENT_TASK.md, .claude/MEMORY.md | PHASE2-001: training_engine/trainers/base_trainer.py |
| 2026-06-20 | PHASE2-001: Base trainer — built `training_engine/trainers/base_trainer.py`: abstract `BaseTrainer` with frozen `TrainerConfig.from_dict()` normalizing the flat backend `training_config` dict (same shape `ConfigBuilder.buildTrainingConfig` produces); constructor takes pre-loaded `dataset_rows` (Celery loads from MinIO — no HF `datasets` import, avoids naming collision landmine). Shared helpers: `resolve_device()` (cuda/mps/cpu), `load_tokenizer()`/`load_model()` (default full-precision causal LM — subclasses override for QLoRA/PEFT), `build_training_arguments()` → HF `TrainingArguments`, `prepare_dataset_rows()` → ChatML via `datasets.formatter.to_chatml` (`dataset_format="unknown"` auto-detects), `get_callbacks()` (MetricsCallback deferred to PHASE2-007). Abstract `train() -> dict`; `TrainerError` on bad methodology/empty dataset/missing config keys. 11 new tests in `tests/test_base_trainer.py` mocking `transformers.AutoTokenizer`/`AutoModelForCausalLM` — full suite 38/38 passing; `uv run ruff check .` clean (reverted unrelated ruff-format drift on pre-existing files). Updated tracking files → PHASE2-002 queued. Pushed `feat/PHASE2-001-base-trainer`. | training_engine/trainers/{base_trainer.py,__init__.py}, training_engine/tests/test_base_trainer.py | PHASE2-002: training_engine/trainers/sft_trainer.py |
| 2026-06-20 | PHASE2-002: SFT trainer — built `training_engine/trainers/sft_trainer.py`: `SFTTrainer(BaseTrainer)` requiring `methodology="sft"`, wires `trl.SFTTrainer` for full-weight updates (no PEFT). `train()` uses base `load_tokenizer`/`load_model`/`build_training_arguments`/`prepare_dataset_rows`, builds HF `Dataset.from_list()` on ChatML rows, passes `formatting_func` via `tokenizer.apply_chat_template`, saves artifacts to `{output_dir}/final`, returns status/metrics dict. Added `utils/hf_datasets.py` (`ensure_hf_datasets_loaded`/`get_hf_dataset_class`) to swap the pip `datasets` package into `sys.modules` before TRL import while preserving local `datasets.formatter`/`datasets.quality_check` submodules — resolves the standing naming-collision landmine for all TRL-based trainers. 3 new tests in `tests/test_sft_trainer.py` mocking TRL/HF components — full suite 41/41 passing; `uv run ruff check .` clean (reverted unrelated ruff-format line-ending drift on pre-existing files). Pushed `feat/PHASE2-002-sft-trainer`. | training_engine/trainers/{sft_trainer.py,__init__.py}, training_engine/utils/hf_datasets.py, training_engine/tests/test_sft_trainer.py | PHASE2-003: training_engine/trainers/lora_trainer.py |
| 2026-06-21 | PHASE2-004: QLoRA trainer — built `training_engine/trainers/qlora_trainer.py`: `QLoRATrainer(LoRATrainer)` requiring `methodology="qlora"`, validates `lora_r`/`lora_alpha` at init via direct `BaseTrainer.__init__` (bypasses `LoRATrainer`'s lora-only guard). Overrides `load_model()` with `BitsAndBytesConfig` 4-bit NF4 (`load_in_4bit=True`, `bnb_4bit_quant_type="nf4"`, `bnb_4bit_compute_dtype=torch.float16`, `bnb_4bit_use_double_quant=True`), then inherits LoRA `train()` ChatML + TRL SFT pipeline. Lazy BnB import via `_get_bitsandbytes_config_cls()`. 4 new tests in `tests/test_qlora_trainer.py` mocking BnB/PEFT/TRL/HF components — full suite 49/49 passing; `uv run ruff check .` clean (formatted only new/touched files). Pushed `feat/PHASE2-004-qlora-trainer`. | training_engine/trainers/{qlora_trainer.py,__init__.py}, training_engine/tests/test_qlora_trainer.py | PHASE2-005: training_engine/trainers/dpo_trainer.py |
| 2026-06-21 | PHASE2-005: DPO trainer — built `training_engine/trainers/dpo_trainer.py`: `DPOTrainer(BaseTrainer)` (subclasses `BaseTrainer` directly, not `LoRATrainer` — `job_service.py`'s required keys for `methodology="dpo"` are just `{"beta"}`, no LoRA), requires `methodology="dpo"`, validates `beta` at init. `train()` calls `load_model()` twice for a policy model + frozen reference-model copy, both wired into `trl.DPOTrainer(model=, ref_model=, beta=, max_length=, max_prompt_length=self.config.max_seq_length // 2, ...)`. Added `prepare_preference_rows()`/`REQUIRED_PAIR_KEYS = ("prompt", "chosen", "rejected")` since DPO rows are preference pairs, not ChatML — `to_chatml()` doesn't apply. `trl` isn't installed in this dev env (confirmed `ModuleNotFoundError`); DPOTrainer's constructor kwargs were taken from trl 0.8.6's known pre-`DPOConfig` API surface rather than live introspection — flagged as a verify-before-reuse risk for PHASE2-006 (ORPO) given trl's API drifted across versions. 5 new tests in `test_dpo_trainer.py` — full suite 54/54 passing; `uv run ruff check .` clean. Pushed `feat/PHASE2-005-dpo-trainer`. | training_engine/trainers/{dpo_trainer.py,__init__.py}, training_engine/tests/test_dpo_trainer.py | PHASE2-006: training_engine/trainers/orpo_trainer.py |
