# DONE — Completed Tasks
# Move tasks here from BACKLOG.md when fully complete and tested.

| ID | Task | Completed | Notes |
|----|------|-----------|-------|
| PHASE1-WEEK1-001 | Project Infrastructure Setup — full directory scaffold, Docker stack (8 services), Next.js 14, FastAPI, training_engine, all services healthy | 2026-06-15 | feat/PHASE1-WEEK1-001-project-scaffold → pushed to origin |
| PHASE1-WEEK1-002 | Makefile Verification + README.md — verified help/ps/logs/lint targets, ruff clean on backend + training_engine, full README written | 2026-06-17 | feat/PHASE1-WEEK1-002-makefile-readme → pushed to origin |
| PHASE1-WEEK2-001 | core/config.py — extended Settings to cover every var in .env.example (App/Postgres/Redis/MinIO/AWS S3/HuggingFace/Cloud GPU Vendors/GPU Pricing/Notifications/Training Engine), added lru_cache get_settings() accessor, switched env_file to an absolute repo-root path so it loads correctly regardless of cwd | 2026-06-17 | feat/PHASE1-WEEK2-001-core-config → pushed to origin |
| PHASE1-WEEK2-002 | core/database.py — async SQLAlchemy engine (create_async_engine on settings.DATABASE_URL, pool_pre_ping=True), AsyncSessionLocal (async_sessionmaker, expire_on_commit=False), declarative Base (DeclarativeBase), get_db() FastAPI dependency yielding AsyncSession and closing in finally | 2026-06-17 | feat/PHASE1-WEEK2-002-core-database → pushed to origin |
| PHASE1-WEEK2-003 | All ORM models — User, Dataset, FineTuneJob, Experiment, ExperimentRun, ModelRegistry as SQLAlchemy 2.0 typed declarative classes (Mapped/mapped_column) inheriting Base from core.database; UUID PKs w/ server_default gen_random_uuid(), JSONB for training_config/quality_report/metrics, cascade-delete FKs to users.id, idx_datasets_user/idx_jobs_user_id/idx_jobs_status/idx_registry_user indexes, relationship() wiring (User.datasets/fine_tune_jobs/experiments/model_registry_entries, etc.); models/__init__.py re-exports all classes for Alembic autogenerate | 2026-06-18 | feat/PHASE1-WEEK2-003-orm-models → pushed to origin |
| PHASE1-WEEK2-004 | Alembic initial migration — wired migrations/env.py to Base.metadata (imports core.database.Base + all model classes), autogenerated + applied initial revision (c631a2aed670) creating all six tables with FKs and indexes against fts_postgres (localhost:5433) via DATABASE_URL_SYNC; fixed idx_datasets_user/idx_jobs_user_id/idx_jobs_status/idx_registry_user which had regressed to default ix_* autogenerated names because they used index=True instead of explicit Index() — replaced with named Index() in __table_args__ on Dataset/FineTuneJob/ModelRegistry; downgrade base → upgrade head round-trip verified; ruff check + format clean | 2026-06-18 | feat/PHASE1-WEEK2-004-alembic-migration → pushed to origin |
| PHASE1-WEEK2-005 | core/security.py — passlib CryptContext(schemes=["bcrypt"], deprecated="auto") with hash_password/verify_password; discovered + fixed a passlib 1.7.4 / bcrypt 5.0.0 incompatibility (passlib's internal detect_wrap_bug self-test raises ValueError on bcrypt>=4.1 instead of truncating), pinned bcrypt==4.0.1 in requirements.txt; 3 new unit tests (hash differs from input, verify succeeds/fails) — 5/5 backend tests pass; ruff check + format clean on new files | 2026-06-18 | feat/PHASE1-WEEK2-005-core-security → pushed to origin |
