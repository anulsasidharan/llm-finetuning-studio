# DONE — Completed Tasks
# Move tasks here from BACKLOG.md when fully complete and tested.

| ID | Task | Completed | Notes |
|----|------|-----------|-------|
| PHASE1-WEEK1-001 | Project Infrastructure Setup — full directory scaffold, Docker stack (8 services), Next.js 14, FastAPI, training_engine, all services healthy | 2026-06-15 | feat/PHASE1-WEEK1-001-project-scaffold → pushed to origin |
| PHASE1-WEEK1-002 | Makefile Verification + README.md — verified help/ps/logs/lint targets, ruff clean on backend + training_engine, full README written | 2026-06-17 | feat/PHASE1-WEEK1-002-makefile-readme → pushed to origin |
| PHASE1-WEEK2-001 | core/config.py — extended Settings to cover every var in .env.example (App/Postgres/Redis/MinIO/AWS S3/HuggingFace/Cloud GPU Vendors/GPU Pricing/Notifications/Training Engine), added lru_cache get_settings() accessor, switched env_file to an absolute repo-root path so it loads correctly regardless of cwd | 2026-06-17 | feat/PHASE1-WEEK2-001-core-config → pushed to origin |
| PHASE1-WEEK2-002 | core/database.py — async SQLAlchemy engine (create_async_engine on settings.DATABASE_URL, pool_pre_ping=True), AsyncSessionLocal (async_sessionmaker, expire_on_commit=False), declarative Base (DeclarativeBase), get_db() FastAPI dependency yielding AsyncSession and closing in finally | 2026-06-17 | feat/PHASE1-WEEK2-002-core-database → pushed to origin |
| PHASE1-WEEK2-003 | All ORM models — User, Dataset, FineTuneJob, Experiment, ExperimentRun, ModelRegistry as SQLAlchemy 2.0 typed declarative classes (Mapped/mapped_column) inheriting Base from core.database; UUID PKs w/ server_default gen_random_uuid(), JSONB for training_config/quality_report/metrics, cascade-delete FKs to users.id, idx_datasets_user/idx_jobs_user_id/idx_jobs_status/idx_registry_user indexes, relationship() wiring (User.datasets/fine_tune_jobs/experiments/model_registry_entries, etc.); models/__init__.py re-exports all classes for Alembic autogenerate | 2026-06-18 | feat/PHASE1-WEEK2-003-orm-models → pushed to origin |
| PHASE1-WEEK2-004 | Alembic initial migration — wired migrations/env.py to Base.metadata (imports core.database.Base + all model classes), autogenerated + applied initial revision (c631a2aed670) creating all six tables with FKs and indexes against fts_postgres (localhost:5433) via DATABASE_URL_SYNC; fixed idx_datasets_user/idx_jobs_user_id/idx_jobs_status/idx_registry_user which had regressed to default ix_* autogenerated names because they used index=True instead of explicit Index() — replaced with named Index() in __table_args__ on Dataset/FineTuneJob/ModelRegistry; downgrade base → upgrade head round-trip verified; ruff check + format clean | 2026-06-18 | feat/PHASE1-WEEK2-004-alembic-migration → pushed to origin |
| PHASE1-WEEK2-005 | core/security.py — passlib CryptContext(schemes=["bcrypt"], deprecated="auto") with hash_password/verify_password; discovered + fixed a passlib 1.7.4 / bcrypt 5.0.0 incompatibility (passlib's internal detect_wrap_bug self-test raises ValueError on bcrypt>=4.1 instead of truncating), pinned bcrypt==4.0.1 in requirements.txt; 3 new unit tests (hash differs from input, verify succeeds/fails) — 5/5 backend tests pass; ruff check + format clean on new files | 2026-06-18 | feat/PHASE1-WEEK2-005-core-security → pushed to origin |
| PHASE1-WEEK2-006 | core/auth.py — create_access_token/create_refresh_token/decode_token via python-jose (jwt.encode/decode against settings.SECRET_KEY + settings.ALGORITHM, exp from ACCESS_TOKEN_EXPIRE_MINUTES/REFRESH_TOKEN_EXPIRE_DAYS), oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login"), get_current_user FastAPI dependency (decodes bearer token, loads User by id from async DB session, raises 401 on missing/invalid/expired/tampered token or unknown user); 5 new unit tests (access + refresh round-trip, expired token, tampered token, wrong signature) — 10/10 backend tests pass; ruff check + format clean on new files (auth.py, test_auth.py) | 2026-06-18 | feat/PHASE1-WEEK2-006-core-auth → pushed to origin |
| PHASE1-WEEK2-007 | core/storage.py — module-level Minio client built from settings.MINIO_HOST/PORT/ROOT_USER/ROOT_PASSWORD/USE_SSL; upload_file/download_file/get_presigned_url(timedelta conversion)/delete_file thin wrappers around put_object/get_object/presigned_get_object/remove_object (minio SDK 7.2.7); no bucket names hardcoded — callers pass the four Settings bucket constants; 4 new unit tests mocking core.storage.minio_client — 14/14 backend tests pass; ruff check + format clean on new files (storage.py, test_storage.py) | 2026-06-18 | feat/PHASE1-WEEK2-007-core-storage → pushed to origin |
| PHASE1-WEEK2-008 | core/celery_app.py — Celery("fts", broker=settings.CELERY_BROKER_URL, backend=settings.CELERY_RESULT_BACKEND) with task_serializer/result_serializer="json", accept_content=["json"], timezone="UTC", enable_utc=True, task_routes pattern-matching tasks.training_tasks.*→training and tasks.export_tasks.*→export queues, task_default_queue="default"; 4 new config-only unit tests (app name, broker/backend resolve from settings, serialization defaults, task_routes mapping) — 18/18 backend tests pass; discovered + fixed a real bug while verifying containers: core/config.py's REPO_ROOT_ENV_FILE = Path(__file__).resolve().parents[3] crashed with IndexError inside Docker (file lives at /app/core/config.py there, only 2 parent levels above root vs 4 locally) — this was crash-looping celery_worker/celery_beat on every startup; guarded the parents[3] lookup so it only resolves when available (containers already get every var via docker-compose's env_file: .env, so the .env file lookup itself is redundant in-container); also added celerybeat-schedule (celery beat's runtime sqlite artifact) to .gitignore; verified docker compose ps shows both celery_worker and celery_beat Up (not Restarting) after the fix, worker log shows clean Redis connection + "ready" | 2026-06-18 | feat/PHASE1-WEEK2-008-celery-app → pushed to origin |
| PHASE1-WEEK2-009 | core/exceptions.py — framework-agnostic AppException(Exception) base carrying status_code/detail, plus NotFoundError(404)/UnauthorizedError(401)/ForbiddenError(403)/ConflictError(409)/ValidationError(422) subclasses, each with a sensible default detail overridable via constructor arg; no FastAPI import so it's safe to raise from core/, services/, or training_engine/ — routes/middleware translate to HTTPException at the API boundary; 15 new parametrized unit tests (default status_code, detail override, isinstance AppException) — 33/33 backend tests pass; ruff check + format clean on new files | 2026-06-18 | feat/PHASE1-WEEK2-009-exceptions → pushed to origin |
| PHASE1-WEEK2-010 | main.py — create_app() FastAPI factory replacing module-level app construction; migrated deprecated @app.on_event("startup") to a lifespan context manager (same file, same task, removed a pytest deprecation warning); added Settings.CORS_ORIGINS (list[str], defaults to localhost:3000/127.0.0.1:3000) so CORS middleware reads from config instead of hardcoded list; new api/v1/api_router APIRouter aggregator wired via app.include_router(api_router, prefix="/api/v1") with a commented example for the auth router landing in PHASE1-WEEK2-012; module-level app = create_app() preserved so tests/conftest.py's `from main import app` still works; 33/33 backend tests pass with zero warnings; ruff check + format clean on all three touched files | 2026-06-18 | feat/PHASE1-WEEK2-010-fastapi-app → pushed to origin |
| PHASE1-WEEK2-011 | GET /health — replaced the static body with real per-dependency checks: PostgreSQL (`SELECT 1` via `AsyncSessionLocal`), Redis (`PING` via `redis.asyncio.from_url(settings.REDIS_URL)`), MinIO (`bucket_exists` on `core.storage.minio_client`), each isolated in its own try/except so one failure can't mask or crash the others; response now reports `database`/`redis`/`storage` as `"up"`/`"down"` plus overall `status: "healthy"|"degraded"`. Discovered during live verification (stopped each dependency container in turn against the host-mapped ports) that the underlying clients' default retry/connect behavior can block the endpoint for 20s+ when a dependency is unreachable (confirmed: MinIO's `bucket_exists` took ~20.5s to raise `MaxRetryError` on DNS failure) — added `_check_with_timeout` wrapping each check in `asyncio.wait_for(..., timeout=5.0)` so the endpoint always responds promptly; verified end-to-end against an unreachable MinIO host that it now returns in ~6s reporting `storage: "down"`, `status: "degraded"` while `database`/`redis` stay `"up"`. Rewrote `tests/test_health.py` (7 tests: 200 status, all-up body, degraded-on-one-down, and one failure-path unit test per dependency mocking the failure point) — full suite 37/37 passing (one `test_auth.py` tampered-token test is flaky/pre-existing, confirmed unrelated by passing in isolation and on rerun). `ruff check .` clean repo-wide; `ruff format --check` clean on both touched files (`main.py`, `tests/test_health.py`) — same 14 pre-existing-drift files noted in prior sessions remain untouched. No new `requirements.txt` dependency needed (`redis==5.0.4` already present from Week 1). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK2-011-health-endpoint`; PR not opened (manual creation per established workflow) | 2026-06-18 | feat/PHASE1-WEEK2-011-health-endpoint → pushed to origin |
| PHASE1-WEEK2-012 | Auth routes — `schemas/auth.py` (UserRegister/UserLogin/TokenResponse/RefreshRequest/UserResponse, Pydantic v2 `from_attributes`); `api/v1/routes/auth.py` wiring `POST /register` (409 `ConflictError` on duplicate email, `hash_password`, creates `User`), `POST /login` (401 `UnauthorizedError` on bad credentials via `verify_password`, issues access+refresh `TokenResponse`), `POST /refresh` (decodes refresh token via `core.auth.decode_token`, re-verifies the user still exists, issues a new token pair), `GET /me` (`Depends(get_current_user)`), and — after confirming scope with the user since CLAUDE.md lists it but it wasn't in the original acceptance criteria — `POST /logout` as a stateless 204 no-op (pure JWT, no blocklist; just requires a valid token). Wired `auth.router` into `api/v1/api_router`. Registered a new `AppException → JSONResponse` exception handler in `main.py` (was missing — without it, `ConflictError`/`UnauthorizedError` would have bubbled up as unhandled 500s instead of their declared `status_code`). Added 13 new tests in `tests/test_auth_routes.py`, exercising the routes for real against the live dev Postgres (no DB mocking) by dogfooding `/register` for setup and cleaning up created rows via sync `psycopg2` (already a dependency, used by Alembic) — avoids ever touching the app's async engine from a second event loop. Hit and fixed a real, structural bug along the way: the `client` fixture in `conftest.py` was function-scoped, so each test spun up its own `TestClient` (and thus its own asyncio event loop) while sharing the single process-wide async SQLAlchemy engine/connection pool from `core/database.py` — connections created in one test's loop got reused/closed from a different test's loop, crashing with `AttributeError: 'NoneType' object has no attribute 'send'` / `RuntimeError: Event loop is closed` on Windows+asyncpg. Fixed by making `client` session-scoped (one event loop for the whole pytest run) — this fixes/enables *any* future test that hits the real DB through the app, not just this task's. Full suite 47/47 passing (verified locally with `DATABASE_URL`/`DATABASE_URL_SYNC` overridden to the host-mapped `localhost:5433`, matching exactly what `.github/workflows/ci.yml` already does for the `backend-test` job — no code needed to support this, it was already CI's pattern). `ruff check .` clean repo-wide; `ruff format --check` clean on every touched/new file (`schemas/auth.py`, `api/v1/routes/auth.py`, `api/v1/__init__.py`, `main.py`, `tests/conftest.py`, `tests/test_auth_routes.py`) — same pre-existing-drift files from prior sessions remain, untouched. Pre-commit hook passed automatically on both commits. Pushed `feat/PHASE1-WEEK2-012-auth-routes` (2 commits); PR not opened (manual creation per established workflow) | 2026-06-18 | feat/PHASE1-WEEK2-012-auth-routes → pushed to origin |
| PHASE1-WEEK2-013 | scripts/seed_data.py + model_catalog/gpu_pricing tables — confirmed scope with the user first (CLAUDE.md's DB schema section listed no catalog/pricing tables yet, and `training_engine/config/model_catalog.py` didn't exist either): chose new DB-backed tables over static config, since the planned `GET /models/catalog` and `GET /gpu/pricing` routes imply queryable data. Added `models/model_catalog.py` (`ModelCatalog`: `model_id` unique/indexed natural key, `display_name`, `family`, `parameter_count_b` as `Numeric(10,2)`, `supports_instruct`) and `models/gpu_pricing.py` (`GpuPricing`: `vendor`+`gpu_type` composite unique index `idx_gpu_pricing_vendor_gpu_type`, `vram_gb`, `price_per_hour_usd` as `Numeric(10,4)`) — both global reference tables with no `user_id` FK. Registered both in `models/__init__.py` and `migrations/env.py`'s explicit import list (needed for autogenerate to see them), autogenerated+applied migration `5d716f49b7ac` (verified `downgrade -1` → `upgrade head` round-trip and inspected the live schema via `psql \d`). Wrote `scripts/seed_data.py` — idempotent via `postgresql.insert(...).on_conflict_do_update(index_elements=[...])` upserts keyed on `model_id` / `(vendor, gpu_type)`; seeds all 13 models from CLAUDE.md's catalog (note: CURRENT_TASK.md's draft acceptance criteria said "12" but the actual CLAUDE.md list has 13 — went with the source of truth) and 10 GPU pricing rows across all 5 vendors (AWS/GCP/Azure/RunPod/Lambda Labs); `structlog` for output, no `print()`. Needed a `sys.path.insert(0, parent)` bootstrap at the top of the script (plus a new `"scripts/*" = [..., "E402"]` entry in `ruff.toml`) because `uv run python scripts/seed_data.py` sets `sys.path[0]` to the script's own directory, not the invocation cwd, so the bare `from core...`/`from models...` imports failed with `ModuleNotFoundError` — same class of fix Alembic already uses via its `prepend_sys_path = .` config. Verified the script runs and re-runs idempotently against the live dev Postgres (13/10 rows both times, confirmed via `psql count(*)`). **Significant test-writing detour, not a code bug:** the obvious test approach — call the async `seed_model_catalog`/`seed_gpu_pricing` functions directly inside `asyncio.run()` — repeatedly crashed with cross-loop asyncpg errors (`Event loop is closed`, `attached to a different loop`), even after consolidating every assertion into a single `asyncio.run()` call and even after adding `engine.dispose()`. Root cause: `conftest.py`'s `client` fixture (session-scoped per the PHASE1-WEEK2-012 fix) holds its own long-lived event loop open for the *entire* test session, and any second ad-hoc `asyncio.run()` that touches the same process-wide `core.database.engine`/connection pool will eventually conflict with it — this codebase's only safe way to touch the real async DB in tests is through that one shared loop. Final fix: `tests/test_seed_data.py` invokes the script via `subprocess.run([sys.executable, "scripts/seed_data.py"], cwd=apps/backend)` (its own fresh engine, in its own process, twice in a row to prove idempotency) and verifies the resulting rows with sync `psycopg2` against `settings.DATABASE_URL_SYNC` — same sync-verification pattern `test_auth_routes.py` already uses for cleanup, fully sidestepping the async engine inside the pytest process. Also hit the now-familiar PostToolUse-hook-strips-import-as-unused gotcha once more (added `engine` to an import line in one `Edit`, its only usage in a separate later `Edit`, hook auto-fixed it away in between) before settling on the subprocess approach. Full suite 50/50 passing (`uv run pytest -q`); `uv run ruff check .` clean repo-wide; `uv run ruff format --check` clean on all 7 touched/new files. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK2-013-seed-data`; PR not opened (manual creation per established workflow) | 2026-06-18 | feat/PHASE1-WEEK2-013-seed-data → pushed to origin |
| PHASE1-WEEK3-001 | Dataset upload — `schemas/dataset.py` (`DatasetResponse`, `from_attributes=True`); `services/dataset_service.py` validates extension (`.json`/`.jsonl`) + content-type whitelist + `settings.MAX_UPLOAD_SIZE_MB` before calling `core/storage.upload_file` (MinIO, `settings.BUCKET_DATASETS`, object key `{user_id}/{uuid4()}_{filename}`), counts rows (`.jsonl`: non-blank line count; `.json`: array length or 1), and creates the `Dataset` row with `format="unknown"` (format detection is PHASE1-WEEK3-002, intentionally not built here per CURRENT_TASK.md's explicit scope note). `api/v1/routes/datasets.py` — `POST /datasets/upload` (multipart, 201) and `GET /datasets` (current user's datasets, newest first), both behind `Depends(get_current_user)`; wired into `api/v1/api_router`. Fixed a real bug caught by the test run, not by inspection: passed `file.file` directly to `upload_file` after already calling `await file.read()` to get `size_bytes`/validate/count rows — the stream was already exhausted at EOF, so MinIO would have received 0 bytes; fixed by uploading `BytesIO(contents)` instead. 5 new tests in `tests/test_dataset_routes.py` (successful upload, oversized via `monkeypatch.setattr(settings, "MAX_UPLOAD_SIZE_MB", 0)`, wrong content-type, unauthenticated 401, list scoped to current user only) — all mock `services.dataset_service.upload_file` so they don't require a live MinIO (CI's `backend-test` job has no MinIO service container, only Postgres+Redis); DB side hits the real dev Postgres exactly like `test_auth_routes.py`. Full suite 55/55 passing; `ruff check .` clean repo-wide; `ruff format --check` clean on every touched/new file (same pre-existing-drift files from prior sessions — `api/__init__.py`, `api/v1/routes/__init__.py`, `api/v1/routes/auth.py`, `schemas/__init__.py`, `schemas/auth.py`, `services/__init__.py` — remain, untouched). Hit the now-familiar PostToolUse-hook-strips-import-as-unused gotcha once more (added `BytesIO` import in one `Edit`, its usage in a separate later `Edit`) — recovered by re-adding both together; also leaked cwd into `apps/backend` once via a bare `cd` during verification, breaking the lint hook, recovered with a plain `cd` back to repo root. Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-001-dataset-upload`; PR not opened (manual creation per established workflow). **Week 3 begins** | 2026-06-18 | feat/PHASE1-WEEK3-001-dataset-upload → pushed to origin |
| PHASE1-WEEK3-002 | Dataset format detector + ChatML normalizer — `training_engine/datasets/formatter.py`: `detect_format(row) -> str` recognizes the three exact shapes from CLAUDE.md section 9 (alpaca: `instruction`/`input`/`output` keys; sharegpt: `conversations` list of `{from, value}`; chatml: `messages` list of `{role, content}`), raising a local `DatasetFormatError(ValueError)` on anything else — no dependency on `apps/backend/core`, since `training_engine` is architecturally a separate standalone process. `to_chatml(row, format=None)` normalizes any of the three to a canonical `{"messages": [{"role", "content"}, ...]}` shape (auto-detecting format when not given) — chosen as the canonical internal representation since that's what `trl.SFTTrainer`/`DPOTrainer` expect; alpaca's `instruction`+`input` combine into one user-turn (`input` omitted entirely when blank), sharegpt's `from` values map `human`→`user`/`gpt`→`assistant`/`system`→`system`. 17 new unit tests in `training_engine/tests/test_formatter.py` (3-format detection, 6 malformed-row cases, alpaca-with/without-input, sharegpt role mapping, chatml passthrough, auto-detect vs explicit format, mismatched explicit format, unsupported format string). **Two real bugs found via verification, not by inspection:** (1) `pytest` was never actually installed in `training_engine`'s `.venv` despite the root `Makefile` already invoking `pytest tests/` for it — added `pytest==8.2.0` to `requirements.txt` and a new `requirements-dev.txt` (mirroring `apps/backend`'s pattern). (2) Running the new tests first failed with `ModuleNotFoundError: No module named 'datasets.formatter'` — root cause: the empty, dead `training_engine/__init__.py` (nothing in the repo imports `training_engine` as a package) made pytest's rootdir package-walk treat `training_engine` itself as a subpackage and continue one level further up to the monorepo root for its `sys.path` insertion; with the repo root (which has no top-level `datasets` package) inserted instead of `training_engine`, `from datasets.formatter import ...` fell through to the *pip-installed* HuggingFace `datasets==2.19.0` library already in `requirements.txt`, which has no `.formatter` submodule. Fixed by deleting the unused `training_engine/__init__.py`; confirmed via `git status` that no other files reference `training_engine.*` anywhere. Flagged (not fixed, out of scope here): the local `training_engine/datasets/` package name will collide with the pip `datasets` (HuggingFace) library the moment a *future* module under `training_engine/datasets/` (e.g. the planned `loader.py`) needs `import datasets` for `load_dataset()` — needs a deliberate decision before PHASE1-WEEK3-003 or later HF-loading work. Full suite 17/17 passing; `uv run ruff check .` clean; `uv run ruff format --check` clean on all touched/new files (pre-existing format drift on the six untouched `training_engine/*/​__init__.py` stub files confirmed via `git status` to be unrelated to this task). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-002-dataset-formatter`; PR not opened (manual creation per established workflow) | 2026-06-18 | feat/PHASE1-WEEK3-002-dataset-formatter → pushed to origin |
| PHASE1-WEEK3-003 | Dataset quality checker — `training_engine/datasets/quality_check.py`: `run_quality_check(rows: list[dict]) -> dict` over canonical ChatML rows (`formatter.to_chatml` output). Made and documented the three scope calls the draft acceptance criteria left open, rather than pausing on them (low-risk, reversible internal choices, consistent with this session's "execute the task completely" instruction): (1) **dedup is exact-match only**, keyed on lowercased/whitespace-collapsed concatenation of all `message.content` values per row — fuzzy/near-duplicate matching deferred as out of scope (real scope increase, no spec for it); (2) **stats are word-count only** (`min`/`max`/`mean`/`median` via plain `.split()`), no tokenizer dependency; (3) **language detection uses `langdetect==1.0.9`** (new pinned dependency, pure-Python, no native/ML deps) with `DetectorFactory.seed = 0` set at import time for deterministic results — langdetect is otherwise seeded from a non-deterministic RNG, which would make any test asserting an exact language code flaky. Report shape: `{total_rows, duplicate_rows, unique_rows, duplicate_indices, word_count: {min,max,mean,median}, languages: {<lang_code>: count}}`; rows with blank/empty text are skipped in language detection (would raise `LangDetectException`) but still counted in `total_rows`. **Resolved the standing `datasets` naming-collision landmine for this task specifically** (flagged since PHASE1-WEEK3-002, see MEMORY.md): `quality_check.py` never needs `import datasets` (the HF library) — dedup/stats/language-detection all operate on already-loaded Python dicts — so the collision simply doesn't trigger here; it remains relevant only for the still-unbuilt `loader.py`. 10 new tests in `training_engine/tests/test_quality_check.py` (no-duplicates baseline, exact duplicate detection, case/whitespace-insensitive dedup, word-count stats on a known small dataset, English detection, French detection, per-language distribution counts, empty-rows-list edge case, blank-row skip in language detection, multi-turn row word counting) — used long, realistic English/French sentences as fixtures since `langdetect` is documented to be unreliable on very short strings, confirmed deterministic via direct REPL checks before locking values into assertions. Full suite 27/27 passing (17 existing formatter tests + 10 new); `uv run ruff check .` clean; `uv run ruff format --check` clean on both new files (confirmed via `git status` that the 3 ruff-flagged pre-existing-drift files — `datasets/__init__.py`, `datasets/formatter.py`, `tests/test_formatter.py` — were untouched this session, same pattern as prior sessions). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-003-quality-check`; PR not opened (manual creation per established workflow) | 2026-06-18 | feat/PHASE1-WEEK3-003-quality-check → pushed to origin |
| PHASE1-WEEK3-004 | Dataset format + quality-check endpoints — confirmed scope with the user first on the genuine architectural fork flagged in CURRENT_TASK.md's blocker (apps/backend and training_engine are separate standalone processes/environments per CLAUDE.md, so apps/backend can't directly import training_engine's formatter/quality_check): chose to **port** the logic into `apps/backend/services/dataset_format.py` and `apps/backend/services/dataset_quality.py` (near-verbatim copies of `training_engine/datasets/formatter.py`/`quality_check.py`) over installing `training_engine` as a backend dependency (would blur the standalone-process boundary and pull `langdetect` + the unresolved `datasets`-naming-collision landmine into a new environment) or dispatching via Celery (real process separation but async-polling complexity for what's a fast, synchronous, CPU-only operation). Added `langdetect==1.0.9` to `apps/backend/requirements.txt` (now a duplicate pin alongside `training_engine/requirements.txt` — expected, since the logic itself is duplicated by design). Extended `services/dataset_service.py` with `get_dataset(dataset_id, user, db)` (404 `NotFoundError` if missing or not owned by the current user), `_parse_rows(contents, filename)` (refactored `_count_rows` to reuse it), `format_dataset(dataset_id, user, db)` (downloads the dataset's raw bytes from MinIO via `download_file`, parses rows, runs `detect_format` on the first row, persists the result to `Dataset.format`, maps `DatasetFormatError` → 422 `ValidationError`), and `quality_check_dataset(dataset_id, user, db)` (downloads + parses rows, normalizes every row via `to_chatml` — using the dataset's already-detected format when available, auto-detecting otherwise — runs `run_quality_check`, persists the report to `Dataset.quality_report` JSONB column). Wired `POST /api/v1/datasets/{id}/format` and `POST /api/v1/datasets/{id}/quality-check` in `api/v1/routes/datasets.py`, both behind `Depends(get_current_user)`, both returning the updated `DatasetResponse` (chose this over a bare report body for response-shape consistency with `/format`). 13 new tests added to `tests/test_dataset_routes.py` via a new `mock_minio_download` fixture (keyed by `storage_path`, mirroring the existing `mock_minio_upload` pattern — no live MinIO needed, consistent with the standing CI rule that `backend-test` has no MinIO service container) plus an `_upload_dataset` helper and an `EN_SENTENCE`/`_alpaca_jsonl` fixture borrowed from `training_engine/tests/test_quality_check.py` for deterministic language detection: format-detects alpaca, rejects unrecognized rows (422), 404 on missing/not-owned dataset for both endpoints, 401 unauthenticated for both, quality-check persists a report with correct `total_rows`/`languages`, and quality-check correctly reuses a previously-detected format. Full suite 64/64 passing; `ruff check .` clean repo-wide; `ruff format --check` clean on all 6 touched/new files (confirmed via `git status` that the same pre-existing-drift files from prior sessions — now 14 files, unrelated to this task — remain untouched). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-004-dataset-endpoints`; PR not opened (manual creation per established workflow) | 2026-06-18 | feat/PHASE1-WEEK3-004-dataset-endpoints → pushed to origin |
| PHASE1-WEEK3-005 | Fine-tune job creation endpoint + config validation — confirmed validation strictness with the user before coding (three options offered: minimal per-method required keys / generic-only / strict full schema per methodology); user chose **minimal per-method required keys**. `schemas/job.py` — `FineTuneJobCreate` (base_model_id, methodology, training_config dict, optional dataset_id/gpu_type/cloud_vendor), `FineTuneJobResponse` (`from_attributes=True`, full ORM field set), `FineTuneJobConfigResponse` (bare `training_config` dict). `services/job_service.py` — `SUPPORTED_METHODOLOGIES` = the six CLAUDE.md section 7 values; `COMMON_REQUIRED_KEYS` = `{learning_rate, num_epochs, batch_size}` for every methodology; `METHOD_SPECIFIC_REQUIRED_KEYS` adds `{lora_r, lora_alpha}` for lora/qlora, `{beta}` for dpo/orpo, `{reward_model_id}` for rlhf (sft has none beyond the common set); unsupported methodology or missing required keys both raise `core.exceptions.ValidationError` → 422 via the existing `AppException` handler. `create_job` reuses `dataset_service.get_dataset(dataset_id, user, db)` verbatim when `dataset_id` is provided, so a missing/not-owned dataset naturally 404s before the job is persisted; new jobs are created with `status="pending"`. Added `get_job`/`list_jobs` (scoped to `FineTuneJob.user_id == user.id`, 404 via `NotFoundError` if missing/not owned). Wired `api/v1/routes/jobs.py` — `POST /jobs` (201), `GET /jobs`, `GET /jobs/{job_id}`, `GET /jobs/{job_id}/config` (reuses `get_job`, response model narrows to just `training_config`), all behind `Depends(get_current_user)`; registered in `api/v1/__init__.py`. 16 new tests in `tests/test_job_routes.py` (success with/without dataset_id, invalid methodology, missing common keys, missing method-specific keys, nonexistent/other-user's dataset_id → 404, unauthenticated → 401, list scoped to current user, get/get-config success + 404 + ownership-scoping + 401) — full suite 80/80 passing. `ruff check .` clean repo-wide; `ruff format --check` clean on all touched/new files (`git status --short` confirmed the pre-existing-drift files from prior sessions were the only ones flagged by a broader format check, and none of them were touched this session). Pre-commit hook passed automatically. Pushed `feat/PHASE1-WEEK3-005-job-creation`; PR not opened (manual creation per established workflow). **Week 3 backend work (PHASE1-WEEK3-001 through 005) complete — frontend tasks (006+) begin next** | 2026-06-19 | feat/PHASE1-WEEK3-005-job-creation → pushed to origin |
| PHASE1-WEEK3-006 | Frontend dashboard shell — confirmed scope with the user first on the one open question (CURRENT_TASK.md flagged `Breadcrumb.tsx`/`PageContainer.tsx` as listed in CLAUDE.md but unused by any page yet): user chose **build `PageContainer` now, defer `Breadcrumb`** until a real page needs it. Read `apps/frontend/AGENTS.md`'s warning that this is Next.js 16 with breaking changes before writing any code, and checked the bundled docs in `node_modules/next/dist/docs/` (layouts-and-pages, project-structure, link) to confirm conventions — `usePathname`, `<Link>`, and route groups all work as expected; noted the new `LayoutProps<'/route'>`/`PageProps<'/route'>` global helper types but skipped them here since they're keyed by literal generated route strings not worth the typegen coupling for a route-group root. Created `app/(dashboard)/layout.tsx` (renders `Sidebar` + `Header` around `children`) and `app/(dashboard)/page.tsx` (minimal placeholder); `components/layout/Sidebar.tsx` (`"use client"` — needs `usePathname` for active-link state — nav links for all 12 CLAUDE.md module routes incl. dashboard home, each with a lucide icon, active state via `aria-current="page"` + `bg-secondary`); `components/layout/Header.tsx` (server component — interactive bits are isolated inside the already-`"use client"` `ui/dropdown-menu.tsx` — placeholder account dropdown with disabled Profile/Settings/Sign out items, real auth wiring deferred to PHASE1-WEEK3-009 per CLAUDE.md); `components/layout/PageContainer.tsx` (max-width/padding wrapper). **Real, pre-existing bug found and fixed, not scope creep:** `apps/frontend/components.json` declares `cssVariables: true` for the `base-nova`/`neutral` shadcn style, but `app/globals.css` only ever had `--background`/`--foreground` — every existing `ui/*` component (`Button`, `Avatar`, `DropdownMenu`, `Separator`, etc., all scaffolded in Week 1) references `border-border`/`bg-muted`/`bg-primary`/etc. that had no underlying CSS custom property, so they'd have rendered unstyled the moment any page actually used them — this task is the first real consumer. Fixed by adding the full standard shadcn neutral token set (`--card`, `--popover`, `--primary`, `--secondary`, `--muted`, `--accent`, `--destructive`, `--border`, `--input`, `--ring`, plus the `--radius-sm/md/lg/xl` scale) to `:root` and the existing `prefers-color-scheme: dark` media query in `globals.css`, and wired them into the `@theme inline` block. Also deleted `app/page.tsx` (the default `create-next-app` scaffold) since `app/(dashboard)/page.tsx` now owns the `/` route — Next.js would otherwise error on two `page.tsx` files resolving to the same path; updated the root `app/layout.tsx`'s `<title>`/description away from the "Create Next App" default while in the file. Verified for real, not just `npm run build`: ran `npm run dev`, fetched `/` over HTTP, confirmed the sidebar/header/page markup and `aria-current="page"` active-link styling were present in the response HTML, then fetched the compiled CSS chunk directly and confirmed `.border-border { border-color: var(--border) }` resolves against a real `--border: #e5e5e5` (light) / `lab(...)`/`#ffffff1a` (dark) value in `:root` — not just that the class names existed. `npm run lint` (ESLint) clean; `npm run build` (Turbopack) compiled successfully with `/` correctly listed as a single static route. Pre-commit hook (ESLint + TypeScript typecheck on frontend) passed automatically. Hit the standing leaked-cwd-breaks-the-lint-hook gotcha once during this session (a `cd apps/frontend && find ...` without subshell parens) — recovered with a plain `cd` back to repo root before the next `Edit`; reinforces the existing rule extends to this codebase's frontend half too. Pushed `feat/PHASE1-WEEK3-006-frontend-layout`; PR not opened (manual creation per established workflow) | 2026-06-19 | feat/PHASE1-WEEK3-006-frontend-layout → pushed to origin |
